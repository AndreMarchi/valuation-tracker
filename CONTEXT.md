# 🧠 Contexto do Projeto: Valuation Tracker

Estou desenvolvendo o Valuation Tracker, uma ferramenta de valuation de ações brasileiras. Continuando de onde parei.

## Stack & Arquitetura

- FastAPI (Python 3.9 via pyenv) + React/TypeScript/Vite/Tailwind CSS
- Backend: porta 8000 | Frontend: porta 5173
- Proxy Vite roteia chamadas `/api` para o backend em dev; em produção o FastAPI serve `/api/*` diretamente
- 3 fontes de dados com fallback automático: **Fundamentus → Brapi → yfinance**
- Cache em memória de 10 minutos
- Selic automática via API BACEN (série 432)
- GitHub: https://github.com/AndreMarchi/valuation-tracker
- **Produção: https://valuation-tracker-production.up.railway.app**

## Métodos de Valuation

- Graham, Bazin, DCF, P/L, P/VP, EV/EBITDA, PEG Ratio, EV/Receita, Rule of 40, DCF Duas Fases
- Score de atratividade 0–10 combinando métodos válidos por setor
- CAPM dinâmico para taxa de desconto (teto 16% / piso 10%)
- WACC dinâmico (equity + dívida com tax shield 34%) (teto 20% / piso 8%)
- FCL calculado via NOPAT (EBIT × (1 - 0.34) × fator setor)
- Análise de risco político/regulatório com penalização no score
- Análise de endividamento (Dívida/EBIT, Dívida/Patrimônio)
- Restrições por setor (bancos, holdings, tech, transporte aéreo, etc.)
- Detecção automática de empresas de crescimento (>15% ao ano)
- Matriz de Consenso — avalia 3 pilares independentes e gera parecer do analista
- **Saúde Financeira via CVM** — score 0–10 com dados trimestrais reais (DRE, DFC consolidados)

## Estrutura de Arquivos Principal

```
backend/
  main.py
  pytest.ini
  .env (BRAPI_TOKEN)
  dados/
    brapi.py
    yfinance_provider.py
    fundamentus_provider.py
    provider.py          ← fallback automático
    normalizacao_setor.py ← normaliza a string de "setor" (ver decisão técnica)
    historico.py
    selic.py
    cvm_provider.py      ← Fase 2.5: lê CSVs da CVM do disco
  valuation/
    graham.py
    bazin.py
    multiplos.py
    dcf.py
    score.py
    risco.py
    setor.py
    endividamento.py
    capm.py
    wacc.py
    nopat.py
    ev_ebitda.py
    crescimento.py
    saude_financeira.py  ← Fase 2.5: score e tendências CVM
    fcfe.py              ← Fase 2.6: FCFE puro (calcular_fcfe, dois estágios)
    fcfe_valuation.py    ← Fase 2.6: orquestração ponta a ponta + sector-aware
  scripts/
    atualizar_cvm.py     ← rodar 1x por trimestre para atualizar dados CVM
  dados_cvm/             ← CSVs da CVM em disco (commitados no repo)
    cad_cia_aberta.csv
    itr_dre_YYYY.csv / itr_dfc_YYYY.csv / itr_bpp_YYYY.csv
    dfp_dre_YYYY.csv / dfp_dfc_YYYY.csv / dfp_bpp_YYYY.csv
  tests/                 ← 267 testes passando
frontend/
  src/
    App.tsx
    types.ts
  vite.config.ts
Dockerfile               ← build multi-stage: Node (frontend) + Python (backend)
railway.toml             ← deploy automático via GitHub push
```

## ✅ O que está funcionando

- Backend FastAPI rodando na porta 8000
- Frontend React/Vite rodando na porta 5173
- 118 testes automatizados passando
- Fundamentus como fonte primária (mais rápido e rico)
- Fallback automático: Fundamentus → Brapi → yfinance
- Selic automática via API BACEN
- CAPM dinâmico por setor
- WACC dinâmico com ponderação real de balanço
- FCL via NOPAT com fatores calibrados por setor
- EV/EBITDA, PEG Ratio, Rule of 40, DCF Duas Fases implementados
- Restrições por setor (bancos, holdings, transporte aéreo)
- Detecção de empresas de crescimento com DCF duas fases
- Análise de risco político e regulatório
- Análise de endividamento com alertas
- Histórico 5 anos com alertas contextuais
- Matriz de Consenso com parecer do analista
- **Watchlist com localStorage** — salvar/remover tickers, cards com score e parecer
- **Botão "Atualizar tudo"** — atualiza todos os tickers da watchlist em paralelo
- **Deploy no Railway** — URL pública, deploy automático a cada git push
- **Saúde Financeira via CVM** — gráficos trimestrais de receita, lucro e FCO
- **Alavancagem e descasamento cambial via CVM** — score de saúde financeira agora penaliza Dívida Bruta/EBITDA alta e descasamento cambial da dívida, com trava crítica (score ≤ 4.5) que não deixa os outros pilares diluírem um risco grave (ex: BEEF3/Minerva antes saía 6/10 sem alerta, hoje cai pra "Risco Elevado / Evitar")
- Endpoints auxiliares: `/cache/clear`, `/selic`, `/api/*` (duplicados para produção)

## 📌 Decisões técnicas tomadas

- `httpx` usa `response.json()` síncrono (sem `await`) — mocks usam `MagicMock` não `AsyncMock`
- Fundamentus retorna LPA, VPA, PL, PVP sem casas decimais (dividir por 100)
- FCL calculado via NOPAT (não mais lucro líquido) — mais preciso por setor
- WACC substitui ajuste manual de taxa de desconto — usa ponderação equity/dívida real
- Selic atual: 14.50% (atualizada automaticamente via BACEN)
- Taxa CAPM teto 16% / piso 10% — WACC teto 20% / piso 8%
- Crescimento negativo em empresa lucrativa → usa mínimo 2% no DCF
- Setores cíclicos limitam crescimento a 8% máximo
- Holdings identificadas por ticker (ITSA3/4) não pelo setor do Fundamentus
- `"from backend import dados"` aparece espontaneamente no `main.py` — remover com: `sed -i '' '1d' main.py`
- Embraer usa ticker `EMBJ3` no Fundamentus (não `EMBR3`)
- Frontend: `App.tsx` refatorado em componentes isolados por seção — fácil de expandir
- **Deploy**: Dockerfile multi-stage — Stage 1: `npm run build` (frontend) → Stage 2: Python + estáticos
- **Produção**: rotas `/api/*` duplicadas no FastAPI (o proxy Vite só funciona em dev)
- **CVM**: bloqueio de IP em servidores — dados baixados localmente via `atualizar_cvm.py` (usa curl) e commitados
- **CVM**: usa dados `_con` (consolidado), não `_ind` (individual) — essencial para holdings e grupos
- **CVM**: `ESCALA_MOEDA = MIL` → valores em milhares → multiplica por 1000 antes de dividir por 1M
- **CVM**: mapeamento ticker → CD_CVM via `cad_cia_aberta.csv` + busca por nome normalizado
- **CVM**: `CD_CVM` no CSV tem zeros à esquerda (ex: `009512`) — usar `.zfill(6)` no filtro
- **Frontend gráficos CVM**: valores `>= 1000M` exibidos em B (bilhões)
- **CVM: bug corrigido em `_extrair_serie()`** — usava `str.startswith(codigo_conta)` (somava conta-pai com contas-filhas, ex: 3.11 + 3.11.01 + 3.11.02) e `groupby().sum()` sem separar trimestre isolado de acumulado no exercício nem remover duplicatas de comparativos entre arquivos (ITR reporta o mesmo `DT_FIM_EXERC` como trimestre E como YTD). Isso inflava receita/lucro/FCO de praticamente todo ticker com dados ITR — corrigido para igualdade exata de conta + manter só a menor duração de período por `DT_FIM_EXERC` + dedup de valores exatos antes de agregar
- **CVM: bug corrigido em `_normalizar_nome()`** — `\bS\.?A\.?\b` não fechava o `\b` final depois de um ponto (não é caractere de palavra), então "MINERVA S.A." virava "MINERVA ." em vez de "MINERVA", fazendo `buscar_cd_cvm` falhar silenciosamente para qualquer nome de `MAPA_NOMES_CVM` terminado em "S.A." — trocado por lookahead `(?=\s|$)`
- **CVM: alavancagem usa Dívida BRUTA, não líquida** — a CVM não baixa o BPA (lado do Ativo do balanço, onde ficariam Caixa/Aplicações Financeiras — só o BPP/Passivo é baixado por `atualizar_cvm.py`). Dívida Bruta = Empréstimos+Financiamentos+Debêntures+Leasing (contas `2.01.04`+`2.02.01`), sempre ≥ dívida líquida, então o erro é sempre conservador (nunca esconde alavancagem). Para Dívida Líquida real seria preciso estender `atualizar_cvm.py` para também baixar `BPA_con` (~250-350MB novos CSVs)
- **CVM: EBITDA = EBIT (conta `3.05`) + D&A TTM (conta `6.01.01.02`, primeira linha de add-back no FCO)** — não inclui depreciação de direito de uso (IFRS16) quando reportada em linha separada, o que subestima levemente o EBITDA (lado conservador do erro)
- **CVM: composição cambial da dívida** vem de `2.01.04.01.01/.02` e `2.02.01.01.01/.02` (só a sub-linha "Empréstimos e Financiamentos", que discrimina moeda — Debêntures não vêm quebradas por moeda nessa taxonomia, mas no Brasil são quase sempre em reais). Empresas 100% financiadas via debênture (ex: CSED3) não têm esse dado — campo fica `None`, sem alerta falso
- **CVM: composição cambial da RECEITA não existe em taxonomia estruturada da CVM** (só em notas explicativas de texto livre) — assumida 0% em moeda estrangeira por padrão; `OVERRIDE_PCT_RECEITA_MOEDA_ESTRANGEIRA` em `cvm_provider.py` permite popular manualmente por ticker quando houver dado publicamente confiável
- **FCFE (`valuation/fcfe.py`) — coleta de inputs via CVM (`buscar_inputs_fcfe_cvm()`)**: lucro líquido (`3.11`) e depreciação/amortização (`6.01.01.02`) reaproveitados das constantes já existentes. ΔCCL vem da conta `6.01.02` ("Variações nos Ativos e Passivos") — confirmado `ST_CONTA_FIXA="S"` (padronizada) e presente em 100% das ~450 empresas checadas em `itr_dfc_2024/2025/2026` e `dfp_dfc_2024/2025`, ao contrário de CAPEX/financiamento (`6.02.XX`/`6.03.XX`, que são `ST_CONTA_FIXA="N"` — nome/posição variam por empresa, precisam de heurística de texto ainda não implementada). Todos os 3 campos são TTM (soma dos últimos 4 trimestres) para ficarem dimensionalmente consistentes entre si
- **FCFE: inversão de sinal do ΔCCL** — a conta CVM `6.01.02` vem na convenção de IMPACTO EM CAIXA (positivo = liberou caixa/capital de giro caiu; é a mesma convenção que a CVM usa pra somar direto ao lucro líquido no cálculo do FCO). `calcular_fcfe()` espera `delta_ccl` na convenção ACADÊMICA de "aumento de capital de giro" (positivo = consumiu caixa) — sinal invertido em relação à conta CVM. `_delta_ccl_convencao_academica()` isola essa troca de sinal numa função só, testável (`+100 → -100`), pra não ficar perdida num comentário
- **FCFE: `test_fcfe.py` tinha `from fcfe import (...)` em vez de `from valuation.fcfe import (...)`** — quebrava a coleta do pytest pra suíte inteira (não só os 14 testes do arquivo); corrigido pra bater com a convenção do resto do projeto
- **FCFE: CAPEX/captação/amortização de dívida implementados via `extrair_por_texto()`** — as contas `6.02.XX` (CAPEX) e `6.03.XX` (financiamento) são `ST_CONTA_FIXA="N"` (não padronizadas): nome, posição E composição variam por empresa, e a MESMA empresa pode reordenar entre filings (confirmado na TAEE3: `6.03.03` é "Emissão de debêntures" — captação — num filing e "Pagamento de Debêntures - Principal" — amortização, sinal oposto — noutro). `extrair_por_texto(df, prefixo, incluir_padroes, excluir_padroes)` casa por `DS_CONTA` (sem acento, case-insensitive) em vez de por código, filtrando linhas-folha (3+ segmentos) dentro do prefixo do grupo. Padrões finais, validados manualmente contra 20 tickers de setores diferentes (óleo&gás, seguros, bolsa, varejo, shoppings, energia, bancos, holding, aluguel de carro, logística, bebidas, carne, educação):
  - **CAPEX** (`imobiliz|intangiv`, exclui `venda|alienac`) — CAPEX bruto, sem netting contra vendas de ativo (definição padrão de DCF/FCFE)
  - **Captação** (`captac|tomad|emiss` **E** `emprest|financiament|debentur` na mesma linha, exclui linhas que começam com `custo`) — "emiss" cobre "Emissão de debêntures" (fraseado real, confirmado na TAEE3), seguro incluir porque a segunda condição já exige um substantivo de dívida, barrando "Emissão de ações" (equity) sozinho
  - **Amortização** (`pagamento|amortiza|liquidad` **E** `emprest|financiament|debentur`, exclui `dividendo`/`arrendamento` e linhas só-de-juros) — "liquidad" cobre "Empréstimos... liquidados" (fraseado real da Minerva/BEEF3, o ticker âncora desta investigação — sem esse padrão a série ficava vazia); a exclusão de juros usa `^(?=.*juros)(?!.*principal)` **com âncora `^` obrigatória** — sem ela, `re.search()` encontra outra posição na string depois de onde "principal" já apareceu, fazendo a exclusão disparar mesmo em linhas combinadas tipo "Pagamento de principal e juros..." (bug real, pego pelo teste automatizado antes de ir pra produção)
- **FCFE: `extrair_por_texto()` NÃO reconsulta `_extrair_serie(df, cd_conta)` por código** — reaproveita o dedup de trimestre/acumulado diretamente sobre o conjunto já filtrado por `DS_CONTA`. Fazer diferente (filtrar por texto pra achar o código certo, depois reconsultar o dataframe inteiro por esse código) mistura contas diferentes que só coincidem numericamente em anos distintos — exatamente o caso da TAEE3 acima. Esse foi o bug mais sério encontrado na validação: antes da correção, WIZC3 e TAEE3 mostravam captação/amortização com sinal invertido (impossível: captação negativa, amortização positiva); depois, 0 anomalias em 20 tickers
- **FCFE: valores suspeitos viram `None`, não um número sabidamente errado** — se a captação sair negativa, ou CAPEX/amortização saírem positivos (deveriam ser sempre saída de caixa), `buscar_inputs_fcfe_cvm()` descarta o valor pra `None` em vez de expor. Confirmado em WIZC3: a própria empresa reporta "Empréstimos tomados" com valor negativo no CSV bruto da CVM (inconsistência real da empresa, não bug do filtro) — sem esse guard, isso viraria uma "nova dívida emitida" negativa, absurdo
- **FCFE: sinal de CAPEX e amortização de dívida** — a CVM reporta as duas como saída de caixa (negativa, mesma convenção do DFC inteiro). `calcular_fcfe()` espera as duas como MAGNITUDE positiva (ver `test_fcfe.py`: `capex=400`, `amortizacao_dividas=300`). `_magnitude_convencao_fcfe()` inverte o sinal, mesmo padrão de `_delta_ccl_convencao_academica()`. Captação (`novas_dividas_emitidas`) não precisa de inversão — a CVM já reporta como entrada positiva, mesma convenção que `calcular_fcfe()` espera
- **FCFE agora fica `fcfe_completo_disponivel: True`** quando os 6 campos existem (validado ponta a ponta contra BEEF3 e CSED3, com `calcular_fcfe()` rodando de verdade) — antes sempre `False`. Ainda fica `False` quando algum campo de texto genuinamente não bateu nenhuma linha pra aquele ticker (ex: PETR4/MRFG3/JBSS3 não tinham captação de dívida no período mais recente disponível — não é falha do filtro, é ausência real de movimento naquele trimestre)
- **BUG PRÉ-EXISTENTE corrigido: TTM via `.tail(4).sum()` cobria ~15 meses, não 12** — `qualidade_lucro` e `divida_bruta_ebitda` (EBITDA) em `buscar_saude_financeira_cvm()` já faziam `_extrair_serie(...).tail(4).sum()` desde a tarefa de alavancagem/câmbio, bem antes do FCFE existir. Causa raiz: o ITR **nunca** reporta o 4º trimestre isolado (só sai via DFP, como total anual) — então `.tail(4)` sobre a série só-ITR pula o Q4 do ano corrente inteiro e inclui o Q1 do ano seguinte pra compensar. Achado durante a investigação do FCFE (mesmo `.tail(4).sum()` ia ser reaproveitado ali), não introduzido por ela. Corrigido com `calcular_ttm_correto(df_itr, df_dfp, codigo_conta)`: reconstrói Q4(ano) = DFP_anual(ano) − [ITR_Q1+Q2+Q3(ano)] só quando os 3 trimestres ITR do ano estão todos presentes (nunca estima Q4 parcial), monta a série trimestral completa e soma os 4 trimestres cronologicamente mais recentes de verdade (pode cruzar virada de ano fiscal, ex: 4T25-derivado+1T26+2T26+3T26). Validado contra BEEF3: a soma do ano fiscal 2025 completo (1T25+2T25+3T25+4T25-derivado) bateu **exatamente** com o lucro anual oficial do fato relevante (R$ 848.260.000, via `dfp_dre_2025.csv`) — confirma a derivação matematicamente. `buscar_saude_financeira_cvm()` (FCO, lucro, EBIT, D&A) e `buscar_inputs_fcfe_cvm()` (lucro líquido, depreciação, ΔCCL) foram refatoradas pra usar a função nova; `tendencia_receita`/`margens_pct`/gráficos trimestrais ficaram como estavam (não são somas TTM, fora do escopo desta correção)
- **TTM corrigido: score do BEEF3 não mudou** — `divida_bruta_ebitda` foi de 4.47x pra 4.94x e `qualidade_lucro` (FCO/Lucro) de 4.5x pra 5.8x com a correção, mas o score final continuou 3.0/"Fraca" (score geral do app: "Risco Elevado / Evitar") — a alavancagem já estava acima de 4x antes e depois, então a trava crítica (`score = min(score, 4.5)`) já capava o resultado nos dois casos. O alerta já estava correto antes desta correção especificamente pro BEEF3; o bug do TTM é real e generalizado (afeta todo ticker com histórico ITR, não só BEEF3), só não mudou o veredito qualitativo deste ticker em particular
- **TTM corrigido: achado à parte durante a validação, fora de escopo** — `ITUB4` (e provavelmente todo banco/instituição financeira) não retorna dado nenhum pra `CONTA_LUCRO_LIQUIDO="3.11"`, nem antes nem depois desta correção. Bancos usam taxonomia de DRE totalmente diferente na CVM (COSIF, não IFRS industrial — `3.01`/`3.02` são "Receitas/Despesas da Intermediação Financeira", `3.05` é "Resultado Antes dos Tributos" em vez de EBIT). Pré-existente, não introduzido agora; as constantes `CONTA_*` deste arquivo nunca foram calibradas pra bancos. `calcular_ttm_correto()` lida com isso graciosamente (retorna `valor: None`, não quebra) — mas a saúde financeira via CVM segue indisponível pra bancos até alguém mapear a taxonomia COSIF separadamente
- **TTM corrigido: validado contra 8 tickers** (BEEF3, PETR4, ITUB4, JBSS3, MRFG3, ABEV3, CSED3, RENT3, MGLU3) — Q4 derivado ficou negativo em 3 casos (PETR4/2024: -R$17bi, MRFG3/2025: -R$42,5mi, CSED3/2024: -R$9,8mi). Não descartados: PETR4/2024 é plausível (Petrobras teve resultado trimestral negativo divulgado publicamente naquele período, provavelmente por impairment) e os outros dois são pequenos relativo ao anual (~6-7%), plausíveis como sazonalidade. Nenhum caso teve magnitude "obviamente absurda" (>3x o anual com sinal invertido) — se aparecer um desses no futuro, vale investigar a demonstração original daquela empresa/ano antes de confiar no valor derivado

- **BUG CORRIGIDO: `CONTA_DEPRECIACAO_AMORTIZACAO` (posição fixa `6.01.01.02`) não é confiável — 32% de divergência, sem concentração setorial.** Auditoria completa contra as ~450 empresas de `itr_dre_2025`/`itr_dfc_2025` (ver auditoria de `CONTA_EBIT`/`CONTA_DEPRECIACAO_AMORTIZACAO` abaixo) encontrou que `ST_CONTA_FIXA="S"` significa apenas "posição obrigatória de preencher", não "sempre o mesmo conceito contábil": a posição `6.01.01.02` diverge em 140/438 empresas (32%), incluindo Petrobras (continha "Encargos, Rendimentos Financeiros..."), Sabesp ("Provisões e Variações Monetárias"), Usiminas ("Encargos e Variações Monetárias/Cambiais"), Grendene ("Resultado de equivalência patrimonial"), Whirlpool ("Lucro antes dos impostos... operações descontinuadas"), Itaú, Vale, CSN Mineração e dezenas de outras empresas "normais", sem nenhum tratamento especial de setor hoje. **Corrigido com `DEPRECIACAO_PREFIXO = "6.01.01"` + `PADRAO_DEPRECIACAO_INCLUIR/EXCLUIR`**, reaproveitando `extrair_por_texto()` (mesmo padrão do CAPEX/financiamento) — casa `deprecia|amortiza|exaust` dentro do bloco de reconciliação, excluindo variações de "amortização" de natureza financeira/tributária/receita (custo de captação/transação de dívida, juros, créditos fiscais, despesas antecipadas, receita diferida — achado ao auditar ~5700 variações reais de DS_CONTA dentro do prefixo). Se nenhuma linha bate, retorna `None` explícito — não cai de volta pra posição fixa. **Validado contra TODAS as ~450 empresas** (não amostra): **227 tiveram o valor de D&A mudado pela correção** (contagem final, já com a correção do bug de acumulado — ver abaixo — aplicada; uma primeira rodada feita antes dessa correção havia contado 231, número corrigido após re-validação; lista completa em `/tmp/dna_texto_mudancas.csv`, gerada durante a validação — não commitada). Âncora Petrobras confirmada: D&A anual DFP 2025 bateu em R$84,39bi, dentro da faixa esperada pelo Form 20-F/SEC (US$15.147mi × câmbio médio ~5,3-5,5 → R$80-83bi esperado, ~1,7% de diferença, plausível dado a aproximação cambial). `buscar_saude_financeira_cvm()` (EBITDA/`divida_bruta_ebitda`) e `buscar_inputs_fcfe_cvm()` (`depreciacao`) foram refatoradas pra usar `calcular_ttm_por_texto()` com os novos padrões, nos dois lugares. **Nota sobre os audits de "% divergência" (este de D&A, 140/438, e o de ΔCCL, 0%)**: são audits de TEXTO/POSIÇÃO (comparam `DS_CONTA` contra um regex esperado, sem soma de TTM) — ortogonais ao bug do acumulado abaixo, continuam válidos como reportados. Já o VALOR de TTM (o que entra em `divida_bruta_ebitda`/`delta_ccl`) é sensível ao acumulado: isoladamente, a correção do acumulado muda o D&A-por-texto de 23 empresas e o ΔCCL de 19 (mesmo conjunto sensível de sempre — Santander, Banco Pine, Banco Daycoval, OI, Camil, etc., ver bug abaixo)

- **BUG CORRIGIDO (mais sério, achado DURANTE a validação do D&A por texto): o DFC no ITR nunca reporta trimestre isolado — só ACUMULADO desde 1º/jan do ano fiscal.** Diferente da DRE (que reporta as duas formas — isolado E acumulado — daí a correção de trimestre/acumulado já documentada em `_extrair_serie()`), o DFC (`DFC_MI_con` — Demonstração de Fluxo de Caixa) só reporta o acumulado: `DT_INI_EXERC` é sempre 1º/jan do ano, mesmo pro "3º trimestre" (que na prática já são 9 meses acumulados). Confirmado sistematicamente: 441 de 451 empresas têm essa característica na conta FCO (`6.01`). Isso significa que `_ttm_a_partir_de_series()` (usada por `calcular_ttm_correto()` e `calcular_ttm_por_texto()`), ao tratar toda série ITR como se fosse sempre isolada — certo pra DRE, mas errado pra qualquer conta de DFC —, estava somando valores acumulados como se fossem isolados. Isso afeta **todo dado que vem do DFC**: FCO (`qualidade_lucro`), D&A (`divida_bruta_ebitda`, ver acima), ΔCCL, CAPEX e financiamento (FCFE inteiro). **Corrigido**: `_extrair_linhas()`/`_extrair_linhas_por_texto()` (renomeadas de `_extrair_serie()`/`extrair_por_texto()`, que viraram wrappers finos que colapsam em `pd.Series` — API pública inalterada) agora preservam `DT_INI` por período; `_ttm_a_partir_de_series()` detecta por trimestre se `DT_INI == 1º/jan` (acumulado, convenção DFC) ou não (isolado, convenção DRE) e isola por diferença contra o acumulado-até-o-trimestre-anterior antes de somar os últimos 4 trimestres. A fórmula generaliza a antiga (quando tudo é isolado, degenera exatamente na fórmula anterior — por isso todos os testes de DRE já existentes continuaram batendo sem alteração)
  - **Achado curioso durante a validação**: a correção deu o MESMO resultado de antes pra Petrobras (TTM de D&A/FCO idêntico) — não por engano, mas por telescopagem matemática: a janela "últimos 4 trimestres" mais comum hoje (2025T2+2025T3+2025T4-derivado+2026T1, dado que a maioria das empresas de calendário-fiscal-dezembro só tem dado até 2026T1 nesta data) faz os termos acumulados intermediários se cancelarem exatamente, resultando na mesma fórmula simplificada (`Anual − Q1 + Q1_ano_seguinte`) nos dois casos. Isso NÃO significa que o bug era inofensivo — é uma coincidência da janela temporal atual. **Validação isolada do impacto real**: comparando a lógica antiga vs nova pra TODAS as ~450 empresas na conta FCO, 22 empresas (as que estão em janelas de trimestres diferentes — dados atrasados, ano fiscal não-dezembro, etc.) mostraram diferença REAL e material, incluindo **Banco Santander (FCO TTM: R$119,4bi → R$64,4bi)**, Banco Pine (R$170M → R$2,03bi), Banco Daycoval (-R$3,27bi → -R$6,87bi) e OI (R$11,6M → -R$529,9M, inverteu de sinal). Essa lista de 22 tickers vai mudar a cada ~3 meses conforme a janela "trimestre mais recente" avança — o bug é estrutural e recorrente, não um evento único; a correção é permanente
  - **Consequência de score**: `qualidade_lucro` (FCO/Lucro) entra direto no score de saúde financeira (±1,5 ou ±0,5 dependendo da faixa — ver `valuation/saude_financeira.py`). Os 22 tickers acima (e qualquer outro que caia numa janela não-invariante no futuro) podem ter o score de saúde financeira mudado por essa correção — vale investigar caso a caso se um usuário perguntar "por que o score do Banco X mudou"
  - **Teste de equivalência**: `test_ttm_correto.py::TestConvencaoAcumuladaDoDfc` prova que a fórmula nova produz resultado idêntico à antiga quando o dado é isolado (equivalência matemática) e isola corretamente quando o dado é acumulado (réplica exata do caso real da Petrobras)

- **Efeito colateral encontrado e corrigido: `MAPA_NOMES_CVM["SBSP3"]` tinha um erro de digitação pré-existente** ("CIA SANEAMENTO BASICO EST SAO PAULO" — "EST" abreviado errado — em vez de "ESTADO"), quebrando silenciosamente a busca da Sabesp na CVM desde que o mapeamento foi criado (não relacionado ao bug de D&A/acumulado, achado ao tentar validar Sabesp como um dos tickers de regressão). Corrigido pra "ESTADO" por extenso — Sabesp agora disponível: `divida_bruta_ebitda=3,34x`, score 7,0/"Boa", 1 alerta de alavancagem elevada (não crítica)

- **Verificação de veredito pós-correção (D&A + acumulado) em tickers com resultado já documentado ou testado**:
  - **BEEF3/Minerva: score/alerta NÃO mudou** (continua 3,0/"Fraca", trava crítica "Risco Elevado / Evitar") — Minerva não estava na lista de 227 tickers com D&A mudado (a posição fixa já era correta pra essa empresa especificamente) nem na lista de 22 tickers afetados pelo bug de acumulado (janela atual é a telescópica invariante)
  - **PETR4/Petrobras: `divida_bruta_ebitda` mudou de 2,41x (errado, D&A subestimado pela posição fixa) pra 1,61x (correto)** — mas as duas caem na mesma faixa neutra de score (1,0x–2,5x, sem pontuação), então o score final não mudou, só o número exibido ficou mais preciso
  - **SBSP3/Sabesp: ficou disponível pela primeira vez** (bug de digitação não relacionado, ver acima) — 7,0/"Boa"
  - **USIM5/Usiminas: `divida_bruta_ebitda` continua `None`** (EBIT TTM atual é negativo — guarda pré-existente de `ebitda_ttm > 0` em `buscar_saude_financeira_cvm()`, não afetada por esta correção) — score 1,0/"Crítica" vem de `qualidade_lucro` negativo (-1,27x) e margem líquida negativa, não de alavancagem

- **`CONTA_EBIT` (`3.05`): decisão — manter posição fixa, documentar como dívida técnica de baixo impacto.** A mesma auditoria completa encontrou divergência em 19/459 empresas (4,1%), mas 95% concentrada em bancos/seguradoras — setores onde o resto do pipeline (`CONTA_LUCRO_LIQUIDO`/`3.11`) já retorna indisponível por taxonomia COSIF (ver nota sobre ITUB4 acima), ou seja, já efetivamente excluídos do pipeline de saúde financeira. Diferente do D&A, o EBIT não tem um padrão de texto limpo e estável tipo `deprecia|amortiza|exaust` — "Resultado Antes do Resultado Financeiro e dos Tributos" tem fraseado muito mais variável entre empresas, tornando uma correção por texto mais arriscada (maior chance de falso-positivo) pra um ganho pequeno (4,1% de divergência, já isolado em setores sem uso prático). Correção adiada até que apareça evidência de impacto real fora de bancos/seguradoras

- **FCFE: integração ponta a ponta concluída** — `valuation/fcfe_valuation.py::calcular_valuation_fcfe()` liga `buscar_inputs_fcfe_cvm()` → `calcular_fcfe()` → `valuation_fcfe_dois_estagios()`, exposto em `dados_finais["fcfe"]` no endpoint `/api/valuation/{ticker}` (mesmo endpoint que já tem a rota `/api/*` duplicada de produção — não foi criada rota separada). Card de frontend `FcfeSection.tsx` renderizado ao lado do card de Cenários DCF.
  - **Ke reaproveitado do CAPM, não recalculado**: `taxa_capm` (já calculado em `main.py` via `calcular_capm()`) é passado direto como `ke` — FCFE desconta a custo de capital PRÓPRIO, não a WACC (`taxa_desconto`), porque o fluxo já é líquido dos efeitos de dívida (diferente do FCFF/DCF, que desconta a WACC)
  - **Crescimento explícito e g_perpetuo: decisão explícita do usuário — espelham o DCF Duas Fases, não o DCF principal.** O app já tinha DOIS DCFs com premissas de crescimento diferentes e nenhuma delas vindo de `crescimento.py` (ambas são constantes/cadeias hardcoded direto em `main.py`): o DCF principal usa uma cadeia de clamps CVM/Fundamentus + `g_perpetuo=0.03` hardcoded; o DCF Duas Fases usa `min(crescimento_5a, 0.30)` (crescimento bruto do Fundamentus) + `g_perpetuo=0.04` hardcoded. Perguntado ao usuário qual delas o FCFE deveria espelhar pra a comparação lado a lado ser consistente — escolhido o **DCF Duas Fases**: `taxa_crescimento_explicito = min(crescimento_5a, 0.30)`, `g_perpetuo = 0.04`, `anos_explicitos = 5` (mesmos valores, mesma fonte, reutilizados literalmente em `main.py`, não recalculados)
  - **Sector-aware via `eh_setor_bancario_ou_segurador()`**: mesmo critério de correspondência parcial de `setor.py::get_configuracao_setor()` contra as 3 chaves financeiras (`Bancos`, `Seguradoras`, `Intermediários Financeiros`) — bancos/seguradoras retornam `{"disponivel": False, "erro": "FCFE indisponível — taxonomia COSIF/SUSEP não suportada"}` **sem sequer consultar a CVM** (curto-circuito antes de `buscar_inputs_fcfe_cvm()`, testado com `assert_not_called()`). Bug pego durante os testes: string vazia é substring de qualquer coisa em Python (`"" in "bancos"` → `True`), então um `setor` vazio/ausente casaria incorretamente como banco — guard explícito adicionado (`if not setor_limpo: return False`). O mesmo padrão de correspondência em `setor.py::get_configuracao_setor()` tinha a MESMA vulnerabilidade pra string vazia — **corrigido depois, numa tarefa pontual separada**: guard `if setor_limpo:` em volta do loop de correspondência parcial, mesma lógica da correção acima. Testes de regressão em `test_setor.py` (`test_setor_vazio_nao_cai_em_classificacao_especifica`, `test_setor_none_nao_cai_em_classificacao_especifica`)
  - **`fcfe_completo_disponivel: False` propaga como indisponibilidade explícita com os campos parciais visíveis** (`inputs_parciais`), não tenta calcular com dado faltando — nem `fcfe_ano_base` nem `projecao` aparecem no retorno nesse caso, e o frontend mostra os campos que já batiam num `<details>` colapsável
  - **Achado real durante a validação end-to-end com BEEF3: FCFE ano-base e valor justo por ação deram NEGATIVOS** (`fcfe = -R$4,97bi`, `valor_justo_por_acao ≈ -R$54 a -R$73` dependendo do dia/janela TTM). Causa: a Minerva fez uma desalavancagem pontual grande no TTM mais recente (amortização de ~R$11,5bi contra captação de ~R$5,9bi, líquido de ~-R$5,6bi) — isso não é bug de extração (mesmos números validados durante a auditoria do ΔCCL/D&A), é uma característica conhecida e inerente do modelo de FCFE em 2 estágios: um evento de financiamento pontual no ano-base é projetado crescendo `(1+g)^n` por 5 anos + perpetuidade, amplificando um evento não-recorrente numa "premissa permanente". O frontend mostra um aviso explícito quando `valor_justo_por_acao <= 0`, orientando a não interpretar como preço-alvo direto nesses casos
  - **Validado manualmente no navegador**: BEEF3 (não-financeiro) mostra o card completo com os números acima; ITUB4 (`setor = "Intermediários Financeiros"`) mostra a mensagem de indisponibilidade, sem quebrar o resto da página
  - Testes em `tests/test_fcfe_valuation.py` (16 testes): fluxo completo com valores reais da BEEF3, `num_acoes` inválido, detecção de setor bancário/segurador (incluindo o guard de string vazia), curto-circuito sem consultar CVM pra bancos/seguradoras, `fcfe_completo_disponivel=False` propagando sem quebrar, CVM totalmente indisponível

- **BUG CORRIGIDO: `enriquecer_com_concessao()` existia mas nunca era chamada** — `main.py::valuation()` retornava `dados_finais` sem nunca passar por essa função, então `resultado["concessao"]` nunca existia na resposta real de `/api/valuation/{ticker}` e o card `ConcessaoSection.tsx` do frontend nunca aparecia, mesmo pra GEPA4/GEPA3 (tickers pros quais o módulo foi construído). **Corrigido**: chamada adicionada antes do `return`, só quando `empresa_tem_concessao(ticker)` é `True` (evita custo extra pra tickers não-concessionários). Achado no caminho: `resultado["metodos"]["dcf_concessao"]` também nunca teria funcionado — `resultado["metodos"]` nunca era inicializado por ninguém — corrigido com `resultado.setdefault("metodos", {})` dentro da própria função. `prob_renovacao`/`desconto_pos_renovacao` (sliders do frontend) agora são query params opcionais na rota, repassados como overrides. Teste de regressão via AST em `test_concessao_wiring.py` (mesmo padrão do `test_capm_wiring.py`) — falha se `enriquecer_com_concessao` deixar de ser chamada dentro de `valuation()`
  - **`ativo_imobilizado` (necessário pro cenário de liquidação do DCF Concessão) não é extraído em lugar nenhum** — a CVM não baixa o BPA (lado do Ativo do balanço), só o BPP (Passivo + Patrimônio Líquido). **Proxy em camadas, implementado em `buscar_capital_investido_proxy_cvm()` (`cvm_provider.py`)**: (1) CVM — Capital Investido = Dívida Bruta + Patrimônio Líquido (ambos já no BPP; pela identidade contábil Ativo=Passivo+PL, é uma superestimativa leve do Imobilizado pra uma concessionária asset-heavy, aceitável pro cenário conservador); (2) se a CVM não tiver dado utilizável, cai pra EV via Fundamentus (valor de mercado + dívida líquida); (3) se nem isso, usa 0.0 — nunca trava a feature. Nota explícita injetada em `concessao.notas` sempre que o proxy real de balanço não foi usado
  - **Achado real, não é bug deste código: GEPA4/Rio Paranapanema Energia (CD_CVM 18368) reporta o BPP consolidado E a DRE inteiramente ZERADOS desde 2024** — confirmado linha a linha (520 linhas do BPP, `VL_CONTA=0` em todas, ITR e DFP, 2023-2025; DRE tem dados reais em 2023, cai pra 0.0 exato a partir de 2024T1). O `ebit_12m` do Fundamentus pra esse ticker também é suspeito (R$56 milhões — muito baixo pra uma geradora de R$3,6bi de valor de mercado). Sintoma de uma mudança na forma de reporte à CVM não relacionada a este projeto — não investigado a fundo (fora de escopo). Consequência: `resultado["concessao"]["preco_justo"]` sai **negativo** pra GEPA4 (-R$6,64), matematicamente correto dado os inputs (dívida líquida de R$1,05bi contra valor de empresa projetado de ~R$418mi), mas não confiável como referência
  - **Regra de exibição pra preço justo <= 0**: `enriquecer_com_concessao()` marca `resultado["concessao"]["confiabilidade_baixa"] = True` e acrescenta uma nota explícita em `notas` quando `preco_justo <= 0` — não esconde o número (é informação real de que algo está errado na fonte, não indisponibilidade de dado), só sinaliza que não deve ser usado como preço-alvo. Testado em `test_concessao_confiabilidade.py` (3 testes, sem rede: usa GEPA4 com inputs artificialmente desbalanceados/balanceados pra forçar preço negativo/positivo deterministicamente)

- **BUG CORRIGIDO: mismatch sistêmico entre a string de "setor" do provedor e as chaves hardcoded em CAPM/NOPAT/crescimento/ciclicos.** Cada um dos 4 pontos que decidem algo por setor (`main.py`/`scanner/trabalhador.py::SETORES_CICLICOS`, `valuation/capm.py::BETA_POR_SETOR`, `valuation/crescimento.py::PSR_MEDIO_SETOR`, `valuation/nopat.py::FATOR_CONVERSAO_NOPAT`) comparava a string de setor por igualdade exata contra sua PRÓPRIA lista de chaves, sem nenhuma normalização compartilhada. Confirmado rodando `buscar_dados()` de verdade contra 11 tickers de setores variados:
  - **"Alimentos Processados"** (BEEF3, JBSS3, BRFS3 — frigoríficos) não batia com a chave `"Alimentos"` em `SETORES_CICLICOS`, `BETA_POR_SETOR` nem `PSR_MEDIO_SETOR` — só `FATOR_CONVERSAO_NOPAT` já tratava as duas strings como sinônimos. Consequência real: o cap de crescimento de setor cíclico (8%) nunca disparava pra esses tickers, e a taxa de crescimento explícita do DCF usava o teto geral de 15% em vez de 8%
  - **"Comércio"** (MGLU3 — varejo) não batia com `"Varejo"` em nenhum dos 4 pontos
  - **Bônus, achado no caminho**: `SETORES_CICLICOS` tinha o literal `"Siderurgia e Siderurgia e Metalurgia"` (erro de digitação/duplicação, nas duas cópias do set — `main.py` e `scanner/trabalhador.py`) — nunca batia com a string real `"Siderurgia e Metalurgia"` (confirmado com CSNA3), mesmo essa string batendo corretamente em `BETA_POR_SETOR` e `FATOR_CONVERSAO_NOPAT`. Corrigido diretamente (não é normalização de provedor, é typo local)
  - **Decisão de escopo**: como o mismatch afetou 2+ setores reais e prominentes (não só Alimentos/Alimentos Processados), optado pela solução ESTRUTURAL em vez de só adicionar chaves duplicadas nos 4 dicionários. **`dados/normalizacao_setor.py::normalizar_setor()`** mapeia a string crua pro nome canônico já usado pelos dicionários mais completos (`BETA_POR_SETOR`/`FATOR_CONVERSAO_NOPAT`), aplicado **uma única vez, dentro de `dados/provider.py::buscar_dados()`** (o ponto de entrada compartilhado por `main.py` e `scanner/trabalhador.py`) — todo o resto do pipeline downstream não precisou ser tocado, só passou a receber a string já canônica. Setores sem mapeamento confirmado (ex: "Máquinas e Equipamentos"/WEGE3, "Diversos"/RENT3) passam inalterados — sem evidência empírica de qual seria o canônico certo, não foi inventado um mapeamento
  - **Impacto real no DCF da BEEF3**: `valor_intrinseco` caiu de R$20,47 (setor não reconhecido como cíclico, crescimento a 15%) pra **R$15,62** (crescimento corretamente capado a 8%) — margem de segurança de 456,25% pra 324,56%. Ainda bem acima do preço de mercado (R$3,68) — mesmo com o EBIT normalizado (média FY23-25, R$2,78bi vs trailing R$3,91bi — ver achado do pico do ciclo do boi acima), o valor cai só pra R$11,09, então o mismatch de setor e o pico de EBIT juntos não fecham o gap sozinhos; resta uma pergunta em aberto sobre a premissa de crescimento/perpetuidade do DCF (não investigada a fundo)
  - Testes em `tests/test_normalizacao_setor.py` (10 testes): mapeamentos confirmados (`Alimentos Processados`→`Alimentos`, `Comércio`→`Varejo`), setores já canônicos passam inalterados, setores sem mapeamento conhecido não são inventados, `None`/vazio não quebra, e confirmação de que os setores normalizados batem de fato nos 4 dicionários downstream (incluindo checagem estática via `ast` de que o typo antigo não existe mais em nenhuma das duas cópias de `SETORES_CICLICOS`)

- **BUGS CORRIGIDOS: 2 mismatches de nome de campo entre backend e frontend** (achados por inspeção de código, sem relação entre si):
  - **EV/EBITDA**: `calcular_ev_ebitda()` recebia `ev_ebitda_setor` como parâmetro mas nunca devolvia esse valor no dict de saída; o frontend lia `ev.ev_ebitda_medio` — uma chave que nunca existiu em nenhum dos dois lados, renderizando `"undefinedx"` cru na tela. Corrigido: backend agora retorna `"ev_ebitda_setor": round(ev_ebitda_setor, 2) if ev_ebitda_setor > 0 else None`; frontend lê `ev.ev_ebitda_setor` com fallback `"—"` quando `null`. Confirmado visualmente: "Média setorial: 8x" pra BEEF3 (antes: "undefinedx")
  - **Endividamento**: backend retorna `"div_liquida_ebit"` (usado também por `score.py` — nome preservado, não renomeado), mas o frontend lia `e.div_ebit` (chave inexistente), caindo em `fmt(undefined,...)` e mostrando `"—"` mesmo com o texto do alerta logo abaixo citando o valor certo (esse texto já vem pré-formatado do backend, por isso não tinha o mesmo bug). Corrigido em `App.tsx` (valor + highlight condicional) e `types.ts`. Confirmado visualmente: "Dívida / EBIT: 3.50x" pra BEEF3 (antes: "—")
  - Tipos TS com campos declarados mas nunca lidos em nenhum componente (`EvEbitda.margem_seguranca`, `Endividamento.div_patrimonio`/`score_penalizacao` — o backend usa `div_liquida_patrim`/`penalizacao`/`score_ajustado`) — não corrigidos por não causarem bug visível (nada os lê), fora do escopo pedido, mencionados aqui como limpeza futura possível
- **Beta de 0.26 pra BEEF3 (Yahoo, 5 anos) — investigado, NÃO é bug do nosso código.** Confirmado chamando a API do Yahoo (via `yahooquery`) diretamente: `summary_detail.get('BEEF3.SA')['beta'] == 0.26` exato, dentro de um payload plausível e consistente (marketCap batendo com o Fundamentus, dividendYield, P/L, 52-week high/low todos coerentes) — é literalmente o que o Yahoo reporta pra esse ticker, sem nenhuma distorção na nossa extração (`dados/provider.py::_buscar_risco_complementar()`). Beta de 5 anos anormalmente baixo é plausível pra uma ação com histórico de eventos idiossincráticos (M&A internacional, reestruturações) que descorrelacionam o preço do movimento do Ibovespa no período. **Nenhum clamp/correção artificial foi implementado** — decisão explícita de não mexer sem confirmação antes

- **WACC: custo da dívida (Kd) agora ajustado por descasamento cambial** — `valuation/wacc.py::calcular_wacc()` ganhou um parâmetro opcional `pct_divida_moeda_estrangeira` (0-100, mesma escala de `cvm_provider.py`), que adiciona um spread ao Kd proporcional à fração da dívida bruta em moeda estrangeira: `spread_cambial = (pct/100) * PREMIO_RISCO_CAMBIAL_PLENO`, com `PREMIO_RISCO_CAMBIAL_PLENO = 0.025` — **mesma ordem de grandeza do Country Risk Premium já usado no CAPM** (`capm.py`, `country_risk = 0.025`), escolhido de propósito pra manter as duas pernas do custo de capital (Ke via CAPM, Kd via WACC) calibradas de forma consistente, em vez de inventar uma constante nova sem relação com o resto do código. Referência qualitativa (não é calibração econométrica formal): o spread histórico entre captação corporativa brasileira em USD vs BRL de risco equivalente costuma ficar na faixa de 1,5-3 p.p., compatível com essa magnitude. `None`/`0` → spread `0`, comportamento idêntico ao de antes — não penaliza ticker sem dado ou sem exposição cambial real
  - Fio até o dado: `main.py` inicializa `dados_cvm = None` e `pct_divida_moeda_estrangeira = None` **antes** do try/except que popula a saúde financeira via CVM (guarda contra o try falhar e deixar a variável indefinida), extrai `dados_cvm.get("pct_divida_moeda_estrangeira")` dentro do mesmo guard já existente (`if dados_cvm and isinstance(dados_cvm, dict)`), repassa pra `calcular_wacc()`
  - **Impacto real na BEEF3** (90% da dívida em moeda estrangeira): Kd de 17,25% → 19,50%, WACC de 13,38% → 14,55%, `valor_intrinseco` do DCF de R$15,62 (pós-fix do setor cíclico) → **R$13,99** (margem de segurança 324,56% → 280,21%)
  - **Confirmado sem penalização indevida** em tickers sem exposição cambial relevante: TAEE3 (`pct_divida_moeda_estrangeira = 0.0` confirmado) e SBSP3/PETR4 (dado indisponível, `None`) — spread exatamente `0` nos três casos, WACC idêntico ao de antes da mudança
  - Escopo: implementado só em `main.py`/`valuation()` (pedido explícito) — `scanner/trabalhador.py::avaliar_ticker()` também chama `calcular_wacc()` mas não foi tocado, continua sem o spread cambial (mesmo comportamento de antes, já que o novo parâmetro é opcional)
  - Testes em `test_wacc.py` (5 novos, 9 no total): `calcular_spread_cambial()` isolada (`None`→0, `0`→0, proporcional a 50%/90%/100%), `calcular_wacc()` sem o argumento novo bate exatamente com o comportamento anterior, cenário com 90% de dívida cambial dá WACC maior que o mesmo cenário sem câmbio (valores conferíveis à mão no comentário do teste)

- **BUG ESTRUTURAL CORRIGIDO: `calcular_dcf()` (DCF principal) entregava Enterprise Value por ação, não Equity Value por ação.** `fluxo_caixa_atual` vem do NOPAT (`valuation/nopat.py`) — exclui juros de propósito, é o fluxo correto pra descontar à WACC e chegar no valor da EMPRESA inteira (dívida + patrimônio). Mas `_calcular_valor()` nunca subtraía a dívida líquida antes de dividir por `num_acoes` — o resultado misturava o que pertence aos credores com o que pertence aos acionistas, e ainda assim era comparado com o preço de mercado (que é preço de equity) pra gerar margem de segurança e classificação "Descontada/Cara". Pra empresas pouco endividadas a distorção é pequena; pra empresas alavancadas, enorme — era provavelmente a maior causa isolada do gap persistente entre o DCF e o preço de mercado observado ao longo de toda essa investigação (maior que o pico do EBIT, o mismatch de setor cíclico e o spread cambial do WACC somados).
  - **Corrigido**: novo parâmetro `divida_liquida: float = 0.0` em `calcular_dcf()` — subtraído do Enterprise Value **antes** de dividir por ação (`valor_equity = valor_total - divida_liquida`). Default `0.0` preserva o comportamento antigo pra qualquer chamada que não passe o argumento. Cenários (otimista/base/pessimista) e matriz de sensibilidade usam a mesma `_calcular_valor()` internamente — corrigidos automaticamente, sem wiring extra, confirmado por teste
  - **Unidade**: `divida_liquida` precisa vir na mesma escala de `fluxo_caixa_atual`/`num_acoes` (R$ milhões) — os dois call sites passam `(dados.get("div_liquida", 0) or 0) / 1_000_000`, igual ao padrão já usado pros outros campos escalados em `main.py`/`scanner/trabalhador.py`
  - **Os DOIS call sites corrigidos de uma vez** (`main.py::valuation()` e `scanner/trabalhador.py::avaliar_ticker()`) — depois de já termos repetido o padrão de esquecer um dos dois call sites duas vezes nesta investigação (bug do CAPM size premium, e o spread cambial do WACC que ficou só em `main.py` por decisão explícita), desta vez os dois foram corrigidos junto e há um teste de paridade AST (`test_dcf_wiring.py`) garantindo isso — mesmo padrão de `test_capm_wiring.py`/`test_concessao_wiring.py`
  - **Impacto real na BEEF3** (Dívida Líquida = R$13.690,3 milhões, ~1.000,54 milhões de ações): `valor_intrinseco` caiu de **R$13,99 → R$0,31** (-97,8%), margem de segurança de +280,21% → **-91,61%**, classificação de "Descontada" → **"Cara"** — a primeira vez nesta investigação inteira que o DCF principal deixa de indicar a BEEF3 como fortemente descontada
  - **Confirmado proporcional à alavancagem, não uma distorção uniforme**: TOTS3 (Dívida Líquida/EBIT 1,5x) caiu 34,5%; WEGE3 (caixa líquido, dívida líquida negativa) **subiu** 8,1% (correto — subtrair um valor negativo soma o caixa de volta ao equity)
  - Testes em `test_dcf.py` (3 novos): `divida_liquida=0.0` idêntico ao comportamento sem o parâmetro (compatibilidade), `divida_liquida>0` reduz o valor na proporção exata (`divida_liquida/num_acoes`, conferido com `pytest.approx`) e propaga pros cenários/matriz, `divida_liquida<0` (caixa líquido) aumenta o valor

- **BUG CORRIGIDO (era "investigação separada", virou correção): `calcular_dcf_duas_fases()` descontava um fluxo de equity (LPA) à WACC, não ao Ke — mesma família de erro do bug do DCF principal, mas na direção oposta (infla, não deflaciona).** LPA já é uma métrica pós-juros/pós-impostos (fluxo de equity, não precisa de subtração de dívida), mas `main.py` passava `taxa_desconto` (a WACC) pra descontá-lo, quando o correto é `taxa_capm` (Ke). Como a WACC de uma empresa alavancada é sempre menor que o Ke (mistura dívida mais barata, pós-tax-shield), descontar um fluxo de equity à WACC inflava o valor justo. **Confirmado só 1 call site em produção** (`main.py:306`, verificado com `git grep`, não presumido — `scanner/trabalhador.py` nunca chamou essa função)
  - **Corrigido**: parâmetro renomeado de `taxa_desconto` pra `ke` em `calcular_dcf_duas_fases()` — nome explícito de propósito (mesmo precedente de `valuation/fcfe_valuation.py::calcular_valuation_fcfe()`), pra esse tipo de erro ficar óbvio na assinatura da função em vez de escondido atrás de um nome genérico já usado pra WACC em outros lugares do código. Call site em `main.py` agora passa `ke=taxa_capm`
  - **Impacto real medido pra BEEF3**: `valor_intrinseco` do DCF Duas Fases caiu de R$11,87 (WACC, errado) pra **R$7,45** (Ke, correto) — inflação de +59,3% eliminada. Ainda assim indica "Descontada" (margem de 102,4%), bem menos otimista que antes mas ainda acima do preço de mercado
  - Testes: `test_crescimento.py` (1 novo — Ke > WACC produz valor estritamente menor, confirma a direção do efeito, não só que mudou) e `test_dcf_duas_fases_wiring.py` (3 testes, AST — confirma que o único call site passa `ke` como keyword E que o valor não é uma variável com nome de WACC tipo `taxa_desconto`/`wacc`, protegendo contra o erro mais sutil de "nome do argumento certo, variável errada")

- **BUG CORRIGIDO: `analisar_endividamento()` recebia `dados.get("fluxo_caixa", 0)` como `patrim_liq` — campo trocado, não dado ausente.** `"fluxo_caixa"` é o lucro líquido TTM mal rotulado (`fundamentus_provider.py`: `fcl = lucro_liq_12m`), nunca foi patrimônio líquido. O patrimônio líquido real já era extraído em todos os 4 providers (`Patrim_Liq`/`Stockholders Equity`/`StockholdersEquity`) mas nunca incluído no dict de retorno — clássico "computa e não expõe", mesmo padrão do bug do `enriquecer_com_concessao()` nunca chamado. Em `scanner/trabalhador.py` a variante era ainda mais sutil: `dados.get("patrliq", dados.get("fluxo_caixa", 0))` tentava primeiro uma chave que nenhum provider jamais retornou (`"patrliq"`), caindo sempre no mesmo valor errado.
  - **Corrigido**: `"patrim_liq"` adicionado ao dict de retorno de `fundamentus_provider.py`, `yfinance_provider.py`, `yquery_provider.py` (variável já existia em todos, só não era exposta) e `brapi.py` (reconstituído como `bookValue × num_acoes`, já que a Brapi só retorna VPA por ação, não o absoluto — `brapi.py` está desconectado da cascata ativa hoje, ver problema conhecido abaixo). Os dois call sites (`main.py` e `scanner/trabalhador.py`) passaram a ler `dados.get("patrim_liq", 0)`. `"fluxo_caixa"` continua existindo e sendo usado normalmente pra tudo mais — só parou de ser usado como substituto de patrimônio líquido
  - **Impacto real medido pra BEEF3** (confirmado contra o Status Invest): Dívida Líquida/Patrimônio Líquido caiu de **19,2x** (dividindo por lucro líquido TTM ≈ R$750,6 milhões) pra **10,86x** (dividindo pelo patrimônio líquido real, R$1.260,4 milhões) — bate exato com o valor de mercado. **O veredito de score não muda**: as duas razões já ultrapassam o limiar de 2x que dispara o alerta `alavancagem_alta`, então o bug distorcia o NÚMERO exibido na tela, não a penalização aplicada ao score final
  - Testes: `test_endividamento.py` (1 novo — mesmos números reais da BEEF3, confirma a razão correta via `pytest.approx(10.86)` e que o valor errado produz uma razão maior, mas o alerta dispara nos dois casos) e `test_endividamento_wiring.py` (AST, novo arquivo — garante que nenhum dos dois call sites usa `"fluxo_caixa"` ou `"patrliq"` como fonte de `patrim_liq`; verificado via stash-revert que pega os dois bugs originais simultaneamente)
  - **Achados adjacentes, sinalizados mas fora do escopo desta correção**: (a) `yfinance_provider.py`/`yquery_provider.py` retornam a chave `"divida_liquida"`, não `"div_liquida"` (que é o que `main.py`/`scanner/trabalhador.py` leem) — mismatch silencioso que só afeta esses dois providers de fallback, já que o Fundamentus (fonte primária) usa a chave certa; (b) `brapi.py` está inteiramente desconectado da cascata ativa (nunca importado por `provider.py`/`main.py`/`scanner/trabalhador.py`, apesar de este CONTEXT.md documentá-lo como "usado apenas como fallback") — o fix de `patrim_liq` aplicado nele não tem efeito em produção até/se alguém o religar

- **BUG CORRIGIDO (4ª ocorrência do mesmo padrão de mismatch de string de setor, agora no setor financeiro/bancário): `valuation/risco.py::SETORES_REGULADOS` só tinha `"Bancos"`, nunca batia com `"Intermediários Financeiros"` — o setor real retornado por `buscar_dados()` pra banco.** Confirmado rodando `buscar_dados()` de verdade pra ITUB4/BBAS3/BBDC4/SANB11: `dados["setor"] == "Intermediários Financeiros"` nos 4; `"Bancos"` é o valor de `dados["industria"]` (subsetor), um campo diferente. Consequência real: bancos não-estatais (ITUB4, BBDC4) nunca recebiam a penalização de risco regulatório de 1,0 ponto que o próprio `SETORES_REGULADOS` já pretendia aplicar (`is_regulado` sempre `False`) — só os estatais (BBAS3, SANB11) eram penalizados, e por outro motivo (`EMPRESAS_ESTATAIS`), não pelo enquadramento regulatório em si.
  - **Os outros 3 pontos que dão tratamento especial ao setor financeiro já batiam corretamente** com o setor real (checados um a um, não presumidos): `valuation/setor.py::CONFIGURACAO_SETORES` tem as duas chaves (`"Bancos"` e `"Intermediários Financeiros"`) e ainda usa correspondência por substring, mais tolerante; `valuation/nopat.py::FATOR_CONVERSAO_NOPAT` só tem `"Intermediários Financeiros"` — que é exatamente o que bate, então **a hipótese de que faltaria essa chave (que motivou investigar) não se confirmou**; `valuation/capm.py::BETA_POR_SETOR` tem as duas chaves mas, verificado via AST, o dicionário **nunca é consultado dentro de `calcular_capm()`** — o beta usado vem de `dados.get("beta")` (o beta real do ticker), não de um lookup por setor. Ou seja, `BETA_POR_SETOR` é código morto pra esse propósito hoje, não um bug de string ativo.
  - **Corrigido**: `"Intermediários Financeiros"` adicionado a `SETORES_REGULADOS`, mesmo padrão defensivo (as duas chaves lado a lado) já usado nos outros 3 dicionários — preferido a normalizar centralmente em `dados/normalizacao_setor.py` porque normalizar pra um único nome canônico quebraria `FATOR_CONVERSAO_NOPAT`, que só tem a chave `"Intermediários Financeiros"` e já funciona hoje
  - **Confirmado no endpoint real**: `valuation()` pra ITUB4 e BBAS3 retorna `dcf.classificacao == "Não aplicável"` e `dcf.valor_intrinseco is None` (não só um número "pequeno" internamente — o card de DCF nunca aparece pro usuário pra banco); pós-fix, `risco.is_regulado` vira `True` e o alerta `"regulatorio"` passa a disparar pra ITUB4/BBDC4 (penalização +1,0), sem duplicar pra BBAS3/SANB11 (já penalizados como estatais)
  - **EV/EBITDA de banco, sinalizado nesta investigação e corrigido na seguinte** (ver duas entradas abaixo)
  - Testes: `test_setor_bancario_wiring.py` (novo arquivo, 7 testes — **chama `buscar_dados()` de verdade** contra ITUB4/BBAS3/BBDC4/SANB11, ao contrário de `test_normalizacao_setor.py` que só testa os mapeamentos já confirmados sem rede; de propósito, já que testes unitários com setor mockado foram exatamente o que não pegou os 3 bugs de string anteriores) — confirma que o setor real bate nos 4 pontos (ou documenta por que não precisa bater, caso do `BETA_POR_SETOR` morto), que o DCF fica "Não aplicável" pro setor financeiro, e que a penalização regulatória dispara pra banco não-estatal. Verificado via stash-revert que pega a regressão original

- **DECISÃO DE MODELAGEM: EV/EBITDA desabilitado pra banco/seguradora, mesma família de Graham/DCF.** Enterprise Value pressupõe separar dívida financeira de operação — pra banco, a "dívida" (depósitos, captações) É a operação, e o D&A que o EBITDA soma de volta é pouco relevante pro resultado (que gira em torno de margem financeira e provisão pra crédito, não ativo fixo). `valuation/setor.py::CONFIGURACAO_SETORES` ganhou `"ev_ebitda"` em `metodos_invalidos` pras chaves `"Bancos"`, `"Intermediários Financeiros"` **e `"Seguradoras"`** (essa última nunca tinha sido incluída, mesma lógica desde sempre, corrigido junto).
  - **Wiring**: `aplicar_restricoes_setor()` ganhou um parâmetro `ev_ebitda: dict = None` — quando `None` (chamador que não calcula EV/EBITDA, ex: `scanner/trabalhador.py`), não faz nada; quando presente e o setor invalida, sobrescreve `classificacao="Não aplicável"`/`preco_justo=None`, mesmo padrão já usado pro `dcf`. **Retorno da função mudou de 5 pra 6 valores** (`graham, bazin, multiplos, dcf, ev_ebitda, config`) — os dois call sites de produção (`main.py`, `scanner/trabalhador.py`) e todos os testes existentes (`test_setor.py`) foram atualizados juntos. Em `main.py`, o cálculo de `ev_ebitda` (que antes vinha DEPOIS da chamada a `aplicar_restricoes_setor()`) foi movido pra ANTES, pra poder alimentar a restrição — `setor_ev` usa o literal `8.0` no lugar de `config_setor.get("ev_ebitda_setor", 8.0)`: essa chave nunca existiu em `CONFIGURACAO_SETORES` (achado incidental), então o `.get(..., 8.0)` sempre caía no default de qualquer forma — comportamento preservado, não uma correção nova
  - **BUG PRÉ-EXISTENTE E NÃO RELACIONADO, achado e corrigido no meio do caminho**: ao verificar o efeito no consenso de bancos, descobrimos que `valuation/multiplos.py::calcular_multiplos()` **nunca retornava uma chave `"classificacao"` agregada no nível raiz** (só `pl.classificacao`/`pvp.classificacao` aninhados) — mas o pilar `"patrimonial_multiplos"` da Matriz de Consenso (`main.py`) lia exatamente essa chave (`multiplos.get("classificacao", "")`), então **sempre** caía em "Não aplicável", pra QUALQUER ticker, não só banco. Sem essa correção, desabilitar EV/EBITDA pra banco deixaria os 3 pilares nulos (`total_metodos_ativos=0`, `"0/0 pilares descontados"`) em vez do `"X/1"` esperado. Corrigido com `classificacao_agregada_multiplos(classificacao_pl, classificacao_pvp)` (nova função exportada em `multiplos.py`): "Descontada" se ambos os válidos concordam, "Cara" se ambos concordam, "Neutra" em sinal misto, "Não aplicável" se nenhum dos dois é válido. Chamada tanto dentro de `calcular_multiplos()` (estado inicial) quanto **recalculada dentro de `aplicar_restricoes_setor()`** depois de zerar pl/pvp por restrição de setor (necessário pro caso "Tecnologia", que invalida P/VP — sem recalcular, a agregada ficaria presa ao valor pré-restrição)
  - **Confirmado sem `ZeroDivisionError`**: `total_metodos_ativos`/`metodos_descontados` nunca entram numa divisão real — só aparecem formatados como string (`f"{x}/{y}"`), então mesmo o caso extremo (`0/0`) é seguro. `calcular_score()` já tinha uma trava separada (`soma_pesos_validos == 0` → retorna "Não foi possível aplicar nenhum método válido", sem crash), confirmada com teste sintético de todas as classificações nulas
  - **Confirmado no endpoint real, pós-fix**: ITUB4/BBAS3 → `ev_ebitda.classificacao = "Não aplicável"`, `dcf.classificacao = "Não aplicável"`, `multiplos.classificacao = "Neutra"` (P/L e P/VP continuam válidos pra banco) → `consenso.pilares_status = {"patrimonial_multiplos": "Neutra", "operacional_ebitda": "Não aplicável", "fluxo_de_caixa": "Não aplicável"}`, `grau_concordancia = "0/1 pilares descontados"` (não `"0/3"` nem `"0/0"`) — score final não quebra (`score.classificacao != "Não aplicável"`, `metodos_aplicados >= 1` nos dois)
  - Testes: `test_multiplos.py` (6 novos — classificação agregada em `calcular_multiplos()` e em `classificacao_agregada_multiplos()` isolada: unânime descontada/cara, sinal misto, um lado não aplicável, ambos não aplicáveis), `test_setor.py` (3 novos — recálculo pós-restrição pro caso Tecnologia/P/VP, EV/EBITDA invalidado pros 3 setores financeiros, `ev_ebitda=None` não quebra), `test_setor_bancario_wiring.py` (2 novos, **endpoint real** — confirma o cenário completo de consenso pra ITUB4/BBAS3, incluindo o texto exato de `grau_concordancia` e que o score não fica nulo). Suíte completa: 295 passed. Verificado via stash-revert (`ImportError` esperado ao reverter `setor.py`/`multiplos.py`/`main.py`) que os testes pegam a regressão

- **DECISÃO DE MODELAGEM: `BETA_POR_SETOR` reativado como fallback SÓ pra beta genuinamente ausente — nunca pra "corrigir" um beta real que pareça estranho.** Achado anterior: `BETA_POR_SETOR` era código morto, `calcular_capm()` sempre recebia `beta_ativo` direto do provider. Decisão explícita: misturar "dado ausente" com "dado real só que surpreendente" (ex: beta de 0,26 da BEEF3, já confirmado como dado genuíno do Yahoo numa investigação anterior, não um erro de extração) seria corrigir dado real sem base sólida — não é isso que se quer.
  - **Problema resolvido primeiro**: antes desta correção, "ausente" e "1.0 real" eram indistinguíveis — `dados/provider.py::_buscar_risco_complementar()`, `yfinance_provider.py` e `yquery_provider.py` caíam num `beta_ativo = 1.0` fixo sempre que o campo não vinha no payload da fonte (`None`/`NaN`), idêntico ao que apareceria se a fonte reportasse 1.0 de verdade. **Corrigido nos 3**: o fallback interno virou `None` em vez de `1.0`, e a chave final do dict (`"beta"`) só aplica `round(float(...), 2)` quando `beta_ativo is not None`, senão propaga `None` mesmo. `brapi.py` não tem chave `"beta"` no retorno (nada a corrigir ali, e o arquivo já está desconectado da cascata ativa, ver achado anterior)
  - **Corrigido**: nova função `resolver_beta(beta_bruto, setor)` em `valuation/capm.py` — `beta_bruto is not None` → devolve ele intacto (nunca troca, mesmo que seja exatamente 1.0 ou um valor "estranho" tipo 0,26); só quando `None` (dado genuinamente ausente) cai em `BETA_POR_SETOR.get(setor, BETA_PADRAO)`. Usada nos dois call sites de produção (`main.py`, `scanner/trabalhador.py`), substituindo `dados.get("beta", 1.0)` por `resolver_beta(dados.get("beta"), setor)` — `calcular_capm()` em si não foi tocada (seu próprio fallback interno pra `beta_ativo=None` continua sendo o 1.0 genérico, comportamento inalterado pra quem chama a função direto, ex: `test_capm.py`)
  - **3 cenários confirmados** (unitário e com dados reais): (a) beta ausente, setor "Intermediários Financeiros" → cai em `0.85` (fallback setorial); setor sem mapeamento → cai em `BETA_PADRAO = 1.0`; (b) beta real `= 1.0` vindo da fonte → permanece `1.0`, não é tratado como ausente; (c) ITUB4 real (`beta = 0.15`) e BEEF3 real (`beta = 0.26`) confirmados via `buscar_dados()` + endpoint completo — `capm.beta` bate exatamente com o valor da fonte, `BETA_POR_SETOR["Intermediários Financeiros"] = 0.85` não interfere em nada
  - Testes: `test_capm.py` (5 novos, `TestResolverBeta` — os 3 cenários + guarda explícita pra beta real `= 0.0`, que é falsy em Python e não pode ser confundido com ausente) e `test_capm_wiring.py` (1 novo, AST — garante que os dois call sites passam `beta_ativo` através de `resolver_beta()`, não de `dados.get("beta", 1.0)` direto). Suíte completa: 301 passed. Verificado via stash-revert (`ImportError` esperado) que os testes pegam a regressão

- **BUG CORRIGIDO: card de Endividamento mostrava "0,0x · sem alertas" pra banco/seguradora, passando a falsa impressão de "sem dívida" — mesma família de Graham/DCF/EV-EBITDA.** `analisar_endividamento()` recebe `ebit_12m` como entrada; pra banco esse campo não é um EBIT operacional de verdade (mesma razão conceitual já usada pra desabilitar Graham/DCF/EV-EBITDA). Com `ebit_12m=0`, a função caía no ramo `else: div_ebit = 0` — pulava todas as penalizações (que só disparam com `div_ebit > 2`) e mostrava "0,0x", que parece um sinal positivo genuíno em vez de "métrica não se aplica".
  - **Confirmado antes de mexer** (não presumido): rastreado o caminho completo de `endividamento.score_ajustado` até o Score de Atratividade final — **são desconectados**. `dados_finais["score"]` vem de `calcular_score()`, calculado ANTES de `analisar_endividamento()` sequer rodar (`main.py`), e nunca é atualizado com o resultado dela; `endividamento.score_ajustado` é puramente informativo. Os números só pareciam "bater" porque `penalizacao=0.0` fazia `score_ajustado = score_atual` por coincidência (mesmo padrão já confirmado antes pra `analisar_risco()`). **Exceção**: no `scanner/trabalhador.py`, `score_atratividade = min(score, risco.score_ajustado, endividamento.score_ajustado)` — ali `endividamento.score_ajustado` PODE puxar o ranking pra baixo (embora, com `penalizacao` sempre `0.0` pra banco mesmo antes da correção, isso nunca aconteceu na prática)
  - **Corrigido**: `"endividamento"` adicionado a `metodos_invalidos` em `valuation/setor.py::CONFIGURACAO_SETORES` pras 3 chaves financeiras (`Bancos`, `Intermediários Financeiros`, `Seguradoras` — mesmo padrão de justificativa dos outros métodos). Em `main.py`/`scanner/trabalhador.py`, quando o setor está na lista, `analisar_endividamento()` **nem chega a rodar** — um dict é montado diretamente: `classificacao="Não aplicável"`, `erro=<justificativa>`, `div_liquida_ebit=None`, `div_liquida_patrim=None`, `alertas=[]`, `penalizacao=0.0`, `score_ajustado=<score de entrada, intacto>`. O ponto crítico (confirmado): o score que entra sai EXATAMENTE igual, sem crédito por "ausência de alerta" nem penalização indevida
  - **Frontend**: `SecaoEndividamento` (`App.tsx`) ganhou um branch cedo pra `classificacao === "Não aplicável"` — mostra a justificativa em texto neutro (mesmo estilo já usado por `SecaoSaudeFinanceira` pro caso `!disponivel`), em vez de cair no `fmt(null, '', 'x')` que mostraria "—" sem contexto nenhum. Tipo `Endividamento` em `types.ts` ganhou o campo opcional `erro?: string`
  - **Confirmado no endpoint real**: ITUB4/BBAS3 → `endividamento.classificacao = "Não aplicável"`, `div_liquida_ebit`/`div_liquida_patrim = null`, `score_ajustado` idêntico ao `score.score` — e o **Score de Atratividade final não mudou nem 0,1 ponto** em nenhum dos dois (ITUB4 7,5 → 7,5; BBAS3 2,5 → 2,5), confirmando a desconexão rastreada acima. BEEF3/WEGE3 continuam com os números reais (`div_liquida_ebit`/`div_liquida_patrim` calculados normalmente) — correção cirúrgica, sem efeito colateral fora do grupo financeiro
  - **Achado adjacente, sinalizado nesta investigação e corrigido na seguinte** (ver entrada abaixo): `SecaoEndividamento` (frontend) tem uma lógica de cor (`highlight`) que espera `classificacao` em `"Crítico"`/`"Alto"`, mas `analisar_endividamento()` retornava strings tipo `"Cara / Evitar"`/`"Neutra"` — nenhuma batia, então a "Classificação" sempre caía no fallback `'green'` (mesmo pra endividamento crítico)
  - Testes: `test_setor.py` (2 novos — `endividamento` invalidado pras 3 chaves financeiras, não invalidado pra setores não-financeiros), `test_setor_bancario_wiring.py` (2 novos, **endpoint real** — confirma `classificacao`/campos nulos/`score_ajustado` preservado pra ITUB4/BBAS3, e que BEEF3/WEGE3 continuam com endividamento real). Suíte completa: 305 passed. Verificado via stash-revert que o teste de config pega a regressão

- **BUG CORRIGIDO: `analisar_endividamento()::classificacao` usava vocabulário de ATRATIVIDADE DE VALUATION ("Muito Atrativa"/"Atrativa"/"Neutra"/"Cara / Evitar", derivado de `score_ajustado`) num card que deveria comunicar RISCO DE ALAVANCAGEM — o frontend (`App.tsx::SecaoEndividamento`) sempre esperou `"Crítico"`/`"Alto"` pra colorir o card de vermelho/amarelo, e essas strings nunca bateram (mismatch frontend/backend, mesma classe de bug já corrigida várias vezes nesta investigação pra setor/setor, agora entre camadas).** Confirmado ao vivo antes de mexer: BEEF3 (Dívida/EBIT 3,5x, Dívida/Patrimônio 10,86x — ambos já níveis de alerta no resto do app) mostrava "Classificação" em cor neutra, nunca em alerta.
  - **Decisão**: corrigir na raiz (backend), não só ajustar o frontend pro rótulo errado — `score_ajustado` mistura o score geral com a penalização de dívida, não é uma leitura direta do nível de alavancagem; o vocabulário certo pra esse card já existia implícito nos `tipo`/`nivel` dos alertas (`endividamento_critico`/`_alto`/`_moderado`, `alavancagem_alta`)
  - **Auditoria feita antes de aplicar** (pedido explícito do usuário): nenhum outro lugar do código (backend, frontend, testes) depende dos valores antigos de `endividamento["classificacao"]` — só `App.tsx::SecaoEndividamento` lia esse campo especificamente. O vocabulário `"Muito Atrativa"`/`"Cara / Evitar"` aparece em outros dois lugares (`valuation/risco.py::classificacao_ajustada` e `ScannerScreen.tsx`'s `a.classificacao`), mas em campos **diferentes** — `risco["classificacao_ajustada"]` (legitimamente um veredito de atratividade ajustado a risco, escopo próprio) e `score["classificacao"]` (o Score de Atratividade geral, via `scanner/trabalhador.py:383` `"classificacao": score.get("classificacao")`) — nenhum dos dois lê `endividamento["classificacao"]`, então não precisaram de ajuste
  - **Corrigido**: `analisar_endividamento()` agora deriva `classificacao` dos mesmos limiares que já definem as penalizações/alertas — `div_ebit > 5` → `"Crítico"`, `> 3` → `"Alto"`, `> 2` → `"Moderado"`, senão `"Saudável"`; comparado com um nível equivalente de `div_patrim` (`> 2` → `"Alto"`, senão `"Saudável"`), usando o **pior dos dois** (`max()` por uma ordem de severidade explícita) — se qualquer uma das duas métricas sinalizar risco alto, a classificação reflete isso, não só a de Dívida/EBIT
  - **Frontend não precisou de nenhuma mudança de código**: `SecaoEndividamento` já esperava exatamente `'Crítico'`/`'Alto'` (o bug estava 100% do lado do backend) — só validação
  - **Confirmado ao vivo, pós-fix**: BEEF3 → `classificacao = "Alto"` (Dívida/EBIT 3,5x → tier "Alto"; Dívida/Patrimônio 10,86x também "Alto" — mesmo resultado pelos dois critérios), renderizado no card com highlight âmbar (confirmado via árvore de acessibilidade — a checagem de cor é comparação de string determinística, `e.classificacao === 'Alto' ? 'amber' : ...`); WEGE3 → `classificacao = "Saudável"` (Dívida/EBIT e Dívida/Patrimônio negativos, caixa líquido), continua verde/neutro, sem regressão
  - Testes: `test_endividamento.py` (5 novos — `classificacao` explícita nos testes já existentes de crítico/saudável/alavancagem-alta, tier "Moderado" isolado, "pior dos dois" quando EBIT e patrimônio discordam, e uma regressão explícita garantindo que `"Muito Atrativa"`/`"Atrativa"`/`"Neutra"`/`"Cara / Evitar"` nunca mais aparecem como `classificacao` de endividamento). Suíte completa: 308 passed

- **FERRAMENTA NOVA: `scripts/auditoria_setorial.py` — auditoria sistemática de mismatch de setor/parâmetro contra TODOS os tickers cobertos, não mais um ticker de cada vez.** Reaproveita `scanner/trabalhador.py::_carregar_tickers_representativos()` pra descoberta (1 ticker por empresa via `dados/setores_b3.json` — **não** via brapi.dev, que está desconectado da cascata, achado anterior) e `main.py::valuation()` como pipeline de cálculo, sem modificar nenhum dos dois. Concorrência via `asyncio.Semaphore(6)` (mesmo limite numérico do `MAX_WORKERS` do Scanner, mecanismo adaptado pro contexto async). Cache próprio (`.auditoria_cache/resultados_<data>.json`) guardando os DADOS BRUTOS por ticker com um fingerprint (mtime máximo de `main.py`+`valuation/`+`dados/`+`scanner/`+o próprio script) — se qualquer um mudar, o cache invalida sozinho e a próxima execução busca de novo, refletindo automaticamente qualquer fix de bug futuro sem precisar de flag manual (`--refresh` força mesmo sem mudança de código).
  - 4 partes: (1) matriz de cobertura setor × dicionário (todos os pontos que indexam por string de setor: `BETA_POR_SETOR`, `EV_EBITDA_MEDIO_SETOR`, `PSR_MEDIO_SETOR`, `SETORES_REGULADOS`, `SETORES_CICLICOS` de `main.py` e de `trabalhador.py` — cópias separadas —, `setor.py::CONFIGURACAO_SETORES` por substring, `nopat.py::FATOR_CONVERSAO_NOPAT` local via chamada real da função); (2) amplitude entre DCF principal/DCF Duas Fases/FCFE; (3) CAGR Receita 5a − CAGR Lucro 5a (indicador do bug de crescimento-de-receita-pra-projetar-lucro); (4) sanity checks (beta fora de [0,15; 2,5], Ke/WACC fora de [8%, 35%], teto de crescimento batido exatamente, dívida negativa suspeita)
  - Rodada contra a base completa (~360 tickers, 327 com sucesso): **97 tickers (~30%) com beta fora de faixa** (não necessariamente bug — small caps podem ter beta extremo real), DOTZ3/BEEF3/MGLU3/ETER3 no topo da divergência DCF×FCFE (amplitudes de centenas a milhares de pp, mesmo padrão FCFE-ano-base-atípico já visto na BEEF3), setor "Gás" identificado como caso concreto do bug de mismatch (ver entrada abaixo)
  - Relatório em markdown, reexecutável (`python3 scripts/auditoria_setorial.py`), gerado em `scripts/auditoria_setorial_relatorio.md`

- **BUG CORRIGIDO (achado pela auditoria setorial): `valuation/wacc.py::calcular_wacc()` lia `dados.get("selic", 0.145)` — fallback hardcoded de 14,5% — e `main.py` NUNCA injetava o Selic real nesse dict antes de chamar a função.** `main.py` já buscava o Selic real via `buscar_selic_atual()` (`selic_val`) e usava corretamente pro CAPM/Ke umas linhas antes, mas nunca repassava esse valor pro WACC. Resultado: o Kd (custo da dívida) do DCF principal em produção sempre usava 14,5% fixo, nunca o Selic real — um bug "silencioso": enquanto o Selic real ficar perto de 14,5% o efeito é pequeno, mas cresce (e some de vista) se o Selic real se afastar bastante desse valor no futuro. `scanner/trabalhador.py` já fazia certo (`dados_wacc["selic"] = selic_val`).
  - **Corrigido**: `main.py` agora cria `dados_wacc = dict(dados); dados_wacc["selic"] = selic_val` antes de `calcular_wacc()` — reaproveita o MESMO `selic_val` já buscado pro CAPM (não busca de novo), garantindo que Ke e Kd nunca podem divergir por causa de uma 2ª chamada com cache expirado no meio da mesma requisição
  - **Impacto real medido** (Selic real 14,25% vs fallback fixo 14,5% — diferença de só 0,25pp): WACC caiu de 13,38% pra 13,25% na BEEF3 (0,13pp), de 17,97% pra 17,95% na VULC3 (0,02pp), praticamente inalterado na WEGE3 (caixa líquido, peso da dívida no WACC ≈ 0). Mas o `valor_intrinseco` do DCF principal da BEEF3 saltou de **R$0,31 pra R$0,47** (+51,6%!) — uma mudança de WACC de só 0,13pp basta pra mover o resultado em mais de 50% numa empresa alavancada, exatamente o tipo de efeito que o próprio bug "escondia" enquanto o Selic real ficasse por perto do valor hardcoded. VULC3 (R$6,70→R$6,71) e WEGE3 (R$10,46→R$10,46) confirmam que o efeito é proporcional à alavancagem, não uniforme
  - Testes: `test_wacc_wiring.py` (novo arquivo, AST — garante que nenhum dos dois call sites passa o dict `dados` bruto pra `calcular_wacc()` sem "selic" injetado, E que o Selic injetado vem da MESMA variável já usada no CAPM, não de uma 2ª busca separada). Verificado via stash-revert que pega a regressão original

- **BUG CORRIGIDO (achado pela auditoria setorial): setor "Gás" (CEGR3/CEG, CGAS3/Comgás, PASS3/Compass) caía em fallback em 8 dos pontos indexados por setor, apesar de "Petróleo, Gás e Biocombustíveis" já ter entradas calibradas nos mesmos dicionários.** Investigado antes de aplicar (pedido explícito): os 3 tickers são **distribuidoras REGULADAS de gás canalizado** (CEG/Comgás são concessionárias locais, Compass é a holding controladora, grupo Cosan) — perfil de concessão regulada de capex intensivo, econômicamente muito mais parecido com **Energia Elétrica** (outra concessão regulada) do que com "Petróleo, Gás e Biocombustíveis" (exploração e produção de commodity, alta ciclicidade — ex: PETR4/PRIO3/RECV3/BRAV3).
  - **Decisão**: mapeado em `dados/normalizacao_setor.py` pra `"Energia Elétrica"`, não pra `"Petróleo, Gás e Biocombustíveis"` — diferente das entradas anteriores desse mapa (Alimentos Processados/Comércio, que eram VARIANTES DE STRING do mesmo conceito), esta é uma aproximação de PERFIL DE RISCO pra um conceito que nenhum dos dicionários hardcoded modela explicitamente ainda (gás canalizado regulado). `setor.py::CONFIGURACAO_SETORES["Energia Elétrica"]` já desabilita DCF clássico por alto capex regulatório — mesma lógica se aplica a gás canalizado
  - **Achado adjacente, sinalizado mas não corrigido**: `PASS3` apresentou beta bruto de 95,57 numa consulta ao vivo (fora de qualquer faixa plausível) — não é um bug de setor, é uma anomalia pontual do dado bruto da fonte (Yahoo/YahooQuery), já contida pelo próprio clamp de segurança existente em `calcular_capm()` (`beta_ativo` fora de `[-1.0, 4.0]` cai pro `BETA_PADRAO = 1.0`), então não afeta o CAPM final. Não investigado a fundo — fica registrado
  - **Confirmado**: `normalizar_setor("Gás") == "Energia Elétrica"`; re-rodando `scripts/auditoria_setorial.py` depois do fix, o setor "Gás" não aparece mais como linha própria na matriz — os 3 tickers se somaram aos 32 já existentes de "Energia Elétrica" (32→35), e o nº de colunas em fallback caiu de 8 pra 3 (mesmo patamar dos outros setores bem cobertos)
  - Testes: `test_normalizacao_setor.py` (2 novos — `normalizar_setor("Gás")`/`("gas")` e confirmação de que bate em `BETA_POR_SETOR`/`FATOR_CONVERSAO_NOPAT_CHAVES`/`CONFIGURACAO_SETORES` e não é tratado como cíclico). Suíte completa: 313 passed

## ⚠️ Problemas conhecidos

- yfinance com rate limit frequente — Fundamentus é a fonte primária
- Histórico 5 anos via yfinance falhando por rate limit
- Brapi tem limitações no plano gratuito (praticamente 1 empresa por conta) — usado apenas como fallback
- CVM bloqueia requests Python/servidor — só funciona via curl com User-Agent de browser
- Dados CVM precisam ser atualizados manualmente 1x por trimestre via `atualizar_cvm.py`
- ~~CVM: só o BPP (Passivo) é baixado, não o BPA (Ativo)~~ — **corrigido na Fase 2 de Novas Metodologias (Valor de Liquidação, ver seção própria abaixo)**: `atualizar_cvm.py` passou a baixar/extrair `BPA_con` também. A alavancagem (`divida_bruta_ebitda`) continua usando Dívida BRUTA de propósito (não é afetada por esta mudança — decisão de escopo já tomada antes, não revisitada)
- CVM: `MAPA_NOMES_CVM` cobre só os tickers problemáticos já mapeados manualmente — tickers fora do mapa dependem do nome vindo do provider (Fundamentus/etc) bater com a razão social oficial da CVM após normalização, o que falha silenciosamente (`disponivel: False`) para vários tickers ainda não mapeados
- **`fco_trimestral`/gráficos trimestrais de DFC podem estar rotulados errado — achado durante a investigação do bug de acumulado, NÃO corrigido ainda.** `buscar_saude_financeira_cvm()` usa `_extrair_serie(dfc, CONTA_FCO)` direto (fora do caminho de TTM) pra montar o gráfico trimestral de FCO exibido no frontend — como o DFC é acumulado (não isolado, ver acima), esses pontos provavelmente representam "acumulado desde 1º/jan" rotulados como se fossem o fluxo daquele trimestre isolado, pra a maioria das empresas. Decisão desta sessão: focar a correção no TTM (que afeta score/alertas, maior gravidade) e deixar o gráfico como dívida técnica separada — mudar o comportamento de um gráfico já exibido é uma decisão de produto (isolar por diferença mudaria os NÚMEROS mostrados no gráfico pra praticamente todo ticker) que não estava no escopo pedido, então precisa de confirmação explícita antes de mexer
- **FCFE: cobertura de captação/amortização varia bastante por ticker** — bancos/seguradoras-holding (ITUB4, BBAS3, BBSE3, CXSE3) não têm captação/amortização de dívida via este filtro (consistente com o gap de taxonomia COSIF já documentado, e agora tratado explicitamente via `eh_setor_bancario_ou_segurador()` — ver decisão técnica de integração do FCFE); mesmo fora de bancos, alguns tickers legitimamente não têm captação no trimestre mais recente disponível (PETR4, MRFG3, JBSS3 no momento desta validação) — `None`/vazio nesses casos é o comportamento correto, não uma falha a corrigir
- **BUG CORRIGIDO (era "hipótese não investigada", agora confirmado com dado de mercado e corrigido): `calcular_dcf_duas_fases()` usava crescimento de RECEITA como proxy de crescimento de LUCRO/LPA.** Em `main.py`, `crescimento_fase1` vinha de `crescimento_5a = dados.get("crescimento_receita_5a", 0)` — crescimento de RECEITA. Mas o fluxo projetado nesse método é o LPA (lucro por ação), pós-margem. Pra empresas com margem comprimindo ou lucro errático, projetar o LPA na taxa de crescimento da receita infla o valor projetado ao ignorar a deterioração de margem no caminho.
  - **Confirmado com dado de mercado real** (Status Invest, BEEF3/Minerva): CAGR de Receita 5 anos = **+23,09%**, CAGR de Lucro 5 anos = **+0,42%** — uma diferença de mais de 22 pontos percentuais, deixando claro que usar um como proxy do outro não é uma aproximação razoável pra essa empresa
  - **Corrigido**: nova função `buscar_crescimento_lucro_anual_cvm()` em `cvm_provider.py` — calcula o CAGR de lucro líquido anual via dados estruturados da CVM (`CONTA_LUCRO_LIQUIDO`, DFP), com uma guarda de segurança específica pra evitar o erro que uma checagem ponta-a-ponta ingênua cometeria: exige que **todos** os exercícios do período sejam positivos, não só os extremos — um CAGR calculado só entre o primeiro e o último ano esconderia um ano de prejuízo no meio (exatamente o caso real da BEEF3: lucro em 2023, prejuízo em 2024, lucro em 2025). Quando não há dado confiável (menos de 2 exercícios, prejuízo em algum ano, empresa não mapeada na CVM), a função retorna `disponivel: False` e `main.py` cai num piso conservador de 2% — nunca volta a usar `crescimento_5a` (reintroduziria o próprio bug). CAGR calculado é clampado em [-5%, +30%] pra não propagar uma variação extrema de 1-2 anos numa projeção de 5
  - **Impacto real medido pra BEEF3**: `valor_intrinseco` do DCF Duas Fases caiu de **R$7,45** (crescimento de receita, 17,7%) pra **R$4,24** (piso conservador de 2%, já que a BEEF3 tem prejuízo em 2024 e não passa na guarda de segurança da CVM) — ainda "Neutra" (margem de 15,2%), bem mais moderado que os +102,4% de antes
  - **Confirmado proporcional ao gap real de cada empresa** (não uma correção uniforme): ABEV3, onde receita (4,6%) e lucro (3,4% via CVM) crescem de forma parecida, teve `valor_intrinseco` de R$7,07 com um ajuste pequeno na taxa; WEGE3, com gap moderado (receita 12,6% vs lucro 7,5% via CVM), teve R$14,48 — a correção é proporcional à distorção real de cada ticker, não um corte arbitrário igual pra todos
  - **Não afeta o score final hoje**: a Matriz de Consenso (`main.py`) usa só o DCF principal (variável `dcf`, pilar `"fluxo_de_caixa"`) pro cálculo do score de atratividade — `calcular_score()` nem recebe `dcf_duas_fases` como parâmetro. O DCF Duas Fases entra só como informação suplementar dentro de `crescimento_info`, exibido na tela mas sem peso no veredito
  - Testes: `test_crescimento_lucro_cvm.py` (6 novos — réplica dos casos reais de BEEF3/prejuízo-no-meio e WEGE3/lucro-consistente, clamp de CAGR extremo, menos de 2 exercícios, empresa não encontrada, DFP vazio) e `test_crescimento.py` (1 novo, AST — garante que `crescimento_fase1` em `calcular_dcf_duas_fases()` nunca referencia `crescimento_5a` diretamente, verificado via stash-revert que pega a regressão)

## 🧮 Novas Metodologias de Valuation (iniciativa separada, em 4 fases)

Iniciativa própria (não confundir com a numeração do Roadmap abaixo, que é
de outra frente): Cenários/Sensibilidade → Valor de Liquidação → SOTP →
Scorecard Qualitativo. Uma fase por vez, com checkpoint ao final de cada uma.

- **Fase 1 — Cenários e Análise de Sensibilidade: ✅ Completa.**
  - `backend/cenarios_sensibilidade.py` — reaproveita `valuation/dcf.py::calcular_dcf()`
    como motor de cálculo (não duplica projeção/desconto de fluxo de caixa).
    `gerar_cenarios()` roda pessimista/base/otimista variando 4 eixos (WACC, g
    perpétuo, margem EBITDA, crescimento de receita) com deltas parametrizáveis
    via `DeltasCenario` (dataclass, defaults documentados, sempre com opção de
    override). `gerar_matriz_sensibilidade()` gera uma matriz 2D genérica pra
    qualquer par de eixos; `gerar_matrizes_padrao()` monta as 3 combinações
    pedidas (WACC×g, WACC×margem EBITDA, margem×crescimento de receita).
  - **Decisão de modelagem — margem EBITDA**: `calcular_dcf()` recebe um
    `fluxo_caixa_atual` já pronto (FCL via NOPAT, ver `valuation/nopat.py`),
    não modela receita/margem separadamente. Simular um cenário de margem
    diferente sem duplicar a cadeia EBIT→NOPAT→FCL foi feito escalando o
    fluxo base proporcionalmente à razão margem_cenário/margem_atual
    (receita implicitamente constante nessa aproximação) — documentado como
    aproximação simples no docstring do módulo, não uma recalibração do NOPAT.
  - **WACC ≤ g perpétuo: erro tratado, não 0.0 silencioso.** `calcular_dcf()`
    sozinho devolve `0.0` nesse caso (bug comum em DCF — parece um "valor
    justo zero" legítimo). `cenarios_sensibilidade.py` levanta
    `WaccInvalidoError` (subclasse de `ValueError`) sempre que uma combinação
    passada DIRETAMENTE (cenário base, pessimista ou otimista) cai em
    WACC≤g — testado inclusive pro caso em que só o delta do cenário
    OTIMISTA (WACC menor, g maior — a direção que aproxima os dois) empurra
    uma combinação válida pra inválida. Já nas CÉLULAS da matriz de
    sensibilidade, uma combinação WACC≤g vira `None` (não exceção) — decisão
    deliberada: as bordas de um heatmap de sensibilidade legitimamente
    incluem combinações extremas fora do domínio (comum em empresas de alto
    crescimento), e `None` já sinaliza "fora do domínio" explicitamente sem
    precisar derrubar a matriz inteira por causa de uma célula de canto.
  - **Endpoint**: `POST /api/valuation/{ticker}/cenarios` (+ `/valuation/{ticker}/cenarios`,
    mesmo padrão de rota duplicada de produção) em `main.py`. Reaproveita
    `buscar_dados()`, `calcular_capm()`/`resolver_beta()`, `calcular_wacc()`
    (mesmo wiring Selic→WACC de `valuation()`, incl. `dados_wacc["selic"] =
    selic_val` a partir da MESMA variável do CAPM) e `calcular_fcl_via_nopat()`
    — todos já existentes, nenhum recalculado do zero. Aceita um body JSON
    opcional (`DeltasCenarioBody`) pra overridar os 4 deltas.
  - **Escopo deliberadamente reduzido nesta preparação de inputs**: o
    endpoint NÃO consulta a CVM (saúde financeira/crescimento de lucro via
    `buscar_saude_financeira_cvm()`/`buscar_crescimento_lucro_anual_cvm()`) —
    usa crescimento de receita do Fundamentus (mesmo piso/teto/regra de
    setor cíclico do DCF principal em `valuation()`), suficiente pros 4 eixos
    de cenário/sensibilidade sem o custo de rede extra da CVM, que não afeta
    nenhum desses 4 inputs. Consequência prática: o `valor_intrinseco` do
    cenário BASE deste endpoint pode divergir um pouco do card "DCF" dentro
    de `/api/valuation/{ticker}` (que usa WACC com spread cambial via CVM) —
    validado ao vivo com BEEF3: card DCF principal = R$0,47 (com spread
    cambial), cenário base deste endpoint = R$2,15 (sem spread cambial, WACC
    13,25% vs ~13,38%+spread). Refatorar `valuation()` pra expor os inputs
    intermediários (e reaproveitar por completo, CVM incluída) é um refactor
    futuro natural, deliberadamente fora do escopo desta fase.
  - **`SETORES_CICLICOS` promovido a constante de módulo** em `main.py` (era
    definida inline dentro de `valuation()`) — reaproveitada pelo novo
    endpoint sem criar uma 3ª cópia literal do set (já existe uma cópia
    separada em `scanner/trabalhador.py`, tech debt pré-existente e não
    tocada). Comportamento de `valuation()` inalterado (mesma verificação,
    só a origem da constante mudou).
  - **Frontend**: `CenariosSensibilidade.tsx` (`components/sections/`) busca
    os próprios dados via `POST /api/valuation/{ticker}/cenarios` (endpoint
    dedicado, fora do payload principal — só este componente faz essa
    chamada). Mostra os 3 cenários, uma barra de faixa de valor com marcador
    do preço atual (e aviso se o preço cair fora da faixa pessimista–otimista),
    e um heatmap com toggle entre as 3 combinações de sensibilidade. Vive ao
    lado do `SecaoCenariosDCF` já existente (o card simples de 3 cenários
    embutido no payload principal) — os dois convivem, não foram unificados
    nesta fase.
  - **`.claude/launch.json` corrigido**: a config `backend-dev` não tinha
    `"cwd": "backend"` — `uvicorn main:app` falhava com `Could not import
    module "main"` ao rodar a partir da raiz do repo (achado ao validar esta
    fase no navegador, não relacionado ao código de cenários em si).
  - **Validado ao vivo com BEEF3** (critério de aceite da fase): pessimista
    R$-5,45 / base R$2,15 / otimista R$16,06, preço de mercado R$3,68 dentro
    da faixa — condiz com o gap DCF-vs-mercado já mapeado nesta investigação
    (ver bugs de setor cíclico/spread cambial/EV→Equity acima). Heatmap WACC×g
    e WACC×margem conferidos célula a célula contra o valor central (bate
    exato com o cenário base) e contra cálculo manual de 1 ano de projeção.
  - Testes: `tests/test_cenarios_sensibilidade.py` (18 novos — cada cenário
    isolado, faixa min/max, deltas customizados não hardcoded, margem base
    zero não quebra por divisão por zero, WACC≤g levanta erro tratado nos 3
    cenários incluindo o caso do delta empurrando só o otimista, dimensões/
    eixos da matriz, célula central bate com `calcular_dcf()` direto, célula
    calculada à mão, células WACC≤g viram `None` sem derrubar a matriz,
    validação de variáveis inválidas, as 3 matrizes padrão, integração
    completa via `gerar_analise_completa()`). Suíte completa: 331 passed
    (313 pré-existentes + 18 novos).
  - **Limitação conhecida**: nenhuma. Critério de aceite cumprido; próximo
    passo natural (não pedido nesta fase) seria uma refatoração de
    `valuation()` em `main.py` pra expor os inputs de DCF já calculados
    (incl. CVM) e o endpoint de cenários reaproveitar 100% deles em vez de
    recalcular WACC/CAPM/crescimento de forma paralela.

- **Fase 2 — Valor de Liquidação: ✅ Completa.**
  - **Bloqueio real encontrado antes de implementar, resolvido com aprovação explícita do usuário**: nenhuma fonte de dados do projeto (CVM incluída) discriminava classes de ativo (caixa, contas a receber, estoques, imobilizado, intangível) — só Patrimônio Líquido e dívida agregados (ver achado already documentado em "Problemas conhecidos": só o BPP/Passivo era baixado da CVM, nunca o BPA/Ativo). Perguntado ao usuário se preferia (a) estender o pipeline da CVM pra baixar o BPA de verdade, ou (b) um proxy agregado sem discriminação por classe — **escolhida a opção (a)**.
  - **`scripts/atualizar_cvm.py` estendido**: `CSVS_NECESSARIOS` ganhou `"BPA_con": "bpa"` (o ZIP da CVM já continha esse CSV, só não era extraído). **Bug de wiring corrigido no caminho**: a checagem de "já atualizado, pular" usava um glob genérico (`{tipo}_*_{ano}.csv`) que batia em QUALQUER CSV existente daquele tipo/ano — adicionar um novo tipo em `CSVS_NECESSARIOS` nunca disparava um novo download pra anos que já tinham DRE/DFC/BPP extraídos (o BPA ficaria pra sempre ausente). Corrigido pra checar a lista exata de arquivos esperados por chave.
  - **Rodado de verdade** (aprovado explicitamente pelo usuário): baixou/extraiu `itr_bpa_2024/2025/2026.csv` e `dfp_bpa_2024/2025/2026.csv` — **+113MB em `backend/dados_cvm/`** (de 361MB pra 474MB), dentro da faixa estimada (~150-250MB). Re-extraiu DRE/DFC/BPP dos mesmos anos no processo (mesmo dado de sempre, só re-baixado junto do BPA na mesma passada).
  - **`dados/cvm_provider.py::buscar_ativos_para_liquidacao_cvm()`** (nova função) — 6 contas do BPA (`1.01.01` Caixa, `1.01.02` Aplicações Financeiras circulante, `1.01.03` Contas a Receber circulante, `1.01.04` Estoques, `1.02.03` Imobilizado, `1.02.04` Intangível) + Passivo Total do BPP (`2.01`+`2.02`, TODAS as obrigações exigíveis — não só a Dívida Bruta financeira já usada em `divida_bruta_ebitda`). Todas com `ST_CONTA_FIXA="S"` (posição padronizada), **validado contra o BPA real da Minerva/BEEF3 (CD_CVM 020931, ITR 2026T1) antes de fixar os códigos** — mesmo nível de rigor já aplicado às contas de BPP existentes.
  - **Decisão de modelagem — só as 6 classes citadas no pedido, não o Ativo Total inteiro**: outras linhas do BPA (Investimentos, Ativo Realizável a Longo Prazo, Ativos Biológicos, Tributos Diferidos etc.) não têm uma convenção de haircut de liquidação citada — incluí-las exigiria inventar um percentual sem base (mesmo princípio já seguido pra contingências, ver abaixo). Mesmo espírito do "net-net working capital" de Graham. Isso torna o piso MAIS conservador, nunca menos — `ativo_total_bpa` e `cobertura_ativo_total_pct` são retornados à parte, de forma transparente, pra deixar claro quanto do Ativo Total real ficou de fora (BEEF3: 89,5% coberto; WEGE3: 90,5%).
  - **`valor_liquidacao.py`** (novo módulo, raiz de `backend/`, mesmo padrão de path de `cenarios_sensibilidade.py`): `HaircutsAtivos` (dataclass, defaults documentados — caixa 100%, aplicações financeiras 90%, contas a receber 80%, estoques 70%, imobilizado 50%, intangível 0%, convenção clássica de liquidation value/Graham, citada no código) + `calcular_valor_liquidacao()` (função pura: Σ ativo×haircut − passivo_total − contingências).
  - **Contingências: input manual opcional, nunca estimado** — a CVM não disponibiliza contingências estruturadas (só em notas explicativas de texto livre, mesma limitação já documentada pra composição cambial da receita). `contingencias=None` (default) aplica 0 e marca `contingencias_informadas=False` no retorno — nunca inventa um valor no lugar de um dado ausente.
  - **Valor negativo NÃO é clampado em zero** — `valor_liquidacao_total`/`_por_acao` podem sair negativos quando o passivo supera os ativos ajustados; `patrimonio_liquido_negativo_em_liquidacao=True` sinaliza explicitamente, mesmo padrão já usado no DCF Concessão pra preço justo ≤ 0 (não esconder o número).
  - **Endpoint**: `GET /api/valuation/{ticker}/liquidacao` (+ `/valuation/{ticker}/liquidacao`), com `contingencias` como query param opcional. Ao contrário do endpoint de Cenários (que levanta 422 em input matematicamente inválido), aqui a ausência de dado da CVM é um estado NORMAL — devolve 200 com `disponivel: False`, mesmo padrão de `saude_financeira`/`fcfe` no payload principal.
  - **Frontend**: o valor de liquidação NÃO virou um componente solto — foi integrado na `FaixaValorBar` já existente de `CenariosSensibilidade.tsx` (Fase 1), como um marcador extra (linha roxa) representando o piso, com legenda própria e aviso quando negativo. O domínio da barra se estende pra baixo automaticamente quando o piso de liquidação fica abaixo do cenário pessimista (caso comum — ver critério de aceite abaixo), senão o marcador ficaria cravado em 0% e escondido atrás do pessimista. Busca própria (`GET /api/valuation/{ticker}/liquidacao`), independente da busca de cenários — uma falha aqui não derruba o card inteiro, só deixa de mostrar o marcador.
  - **Critério de aceite confirmado ao vivo**: BEEF3 → valor de liquidação **R$-16,66/ação**, abaixo do cenário pessimista de Cenários&Sensibilidade (R$-5,45) — Minerva é uma empresa muito alavancada (Passivo Total R$39,8bi vs. Ativo ajustado com haircut de só R$23,1bi), ficaria patrimonialmente negativa numa liquidação forçada, consistente com o rating já existente de "Risco Elevado/Evitar". WEGE3 → valor de liquidação **R$0,43/ação**, muito abaixo do pessimista (R$7,52) — WEG negocia a um múltiplo alto sobre o valor patrimonial (empresa de crescimento/rentabilidade, não uma "value trap" de ativos), então o piso de liquidação sendo baixo é o resultado esperado, não um problema. Os dois casos confirmam o critério ("valor de liquidação abaixo do pessimista na maioria dos casos testados") sem precisar forçar nada.
  - Testes: `tests/test_valor_liquidacao.py` (11 novos — cálculo passo a passo com balanço sintético conhecido, contingências `None` vs. `0.0` explícito, haircuts customizados, passivo maior que ativos ajustados dá valor negativo não-clampado, `num_acoes=0` não quebra) e `tests/test_liquidacao_cvm_inputs.py` (6 novos — extração via BPA/BPP mockado, escala `MIL` aplicada corretamente, empresa não encontrada/BPA vazio/BPP vazio/tudo zerado retornam `disponivel: False`, classe ausente vira `0.0` não `None`). Suíte completa: **348 passed** (331 pré-existentes + 17 novos).
  - **Limitação conhecida**: as classes de ativo cobrem só a parcela CIRCULANTE de Aplicações Financeiras/Contas a Receber/Estoques — as versões de longo prazo (dentro de "Ativo Realizável a Longo Prazo", `1.02.01.02/.04/.05`) não são somadas. Na Minerva essas linhas vieram zeradas (não afetou a validação), mas uma empresa com valores materiais nessas posições teria o piso de liquidação levemente subestimado — na direção conservadora (nunca infla o piso), então não é tratado como bug, só registrado como possível refinamento futuro.

- **Fase 3 — SOTP (Soma das Partes): ✅ Completa.**
  - **`sotp.py`** (novo módulo, raiz de `backend/`, mesmo padrão de path de `cenarios_sensibilidade.py`/`valor_liquidacao.py`): `Segmento` (dataclass — nome, método, e só os campos relevantes daquele método) + `calcular_ev_segmento()` + `ConfiguracaoSotp` (segmentos, dívida líquida consolidada, num_ações, desconto de holding) + `calcular_sotp()`.
  - **3 métodos por segmento**: `"ev_ebitda"`/`"ev_receita"` são multiplicações diretas (EV = métrica × múltiplo) — **decisão explícita de NÃO reaproveitar** `valuation/ev_ebitda.py::calcular_ev_ebitda()` nem `valuation/crescimento.py::calcular_ev_receita()`: essas duas embutem lógica de EMPRESA INTEIRA (blend 50/50 empresa/setor, divisão por ações, subtração de dívida) que não se aplica a um segmento isolado — forçar o reuso exigiria parâmetros fictícios escondendo uma lógica de blend que não faz sentido no nível de segmento. Já `"dcf"` **reaproveita de verdade** `valuation/dcf.py::calcular_dcf()` (`num_acoes=1.0`/`divida_liquida=0.0`, fazendo `valor_intrinseco` degenerar exatamente no Enterprise Value bruto do segmento) — a projeção/desconto multi-ano É lógica não-trivial que não deveria ser duplicada.
  - **WACC do segmento ≤ g perpétuo levanta erro tratado** (mesma trava de `cenarios_sensibilidade.py::WaccInvalidoError`, sem repetir o "0.0 silencioso" de `calcular_dcf()` isolado) — um segmento inválido não trava o SOTP inteiro: entra em `segmentos_com_erro`, os demais segmentos continuam somados normalmente.
  - **Desconto de holding: parâmetro explícito (`desconto_holding_pct`), nunca embutido silenciosamente** — aplicado uma única vez sobre o Equity consolidado (EV somado − dívida líquida consolidada), não por segmento. Dívida líquida também é SEMPRE consolidada — nunca alocada por segmento (evita dupla contagem/arbitrariedade de rateio entre braços de negócio).
  - **Mecanismo de input manual**: `dados/sotp_config.json` (JSON keyed por ticker, mesmo espírito do cache JSON diário do Scanner e do padrão já usado em `CONCESSOES_CONHECIDAS`/`OVERRIDE_PCT_RECEITA_MOEDA_ESTRANGEIRA`) — nenhuma tentativa de inferir segmentos automaticamente, já que nenhum provider do projeto reporta dado por unidade de negócio.
  - **Exemplo de configuração: SIMH3 (Simpar), com valores ILUSTRATIVOS** — decisão tomada após avaliar ITSA4 (Itaúsa) como exemplo mais "óbvio" de holding com braço financeiro: descartado porque o braço financeiro (participação no Itaú Unibanco) exigiria `"ev_ebitda"` ou `"dcf"` pra um NEGÓCIO BANCÁRIO — mesma inconsistência metodológica que este projeto já rejeitou explicitamente pro DCF/EV-EBITDA de bancos em geral (ver "Problemas conhecidos"/decisões técnicas sobre setor financeiro acima). SIMH3 evita o problema (segmentos 100% industriais/serviços: Vamos, Movida, JSL) sem abrir mão de ser uma holding real e reconhecível da B3. Os valores de EBITDA/múltiplo no JSON são ILUSTRATIVOS (não são os números reais/auditados da Simpar) — documentado explicitamente no próprio arquivo (`_aviso`) e aqui.
  - **Endpoint**: `POST /api/valuation/{ticker}/sotp` (+ `/valuation/{ticker}/sotp`) — aceita a configuração de segmentos no corpo da requisição (override ad-hoc) OU carrega de `dados/sotp_config.json` quando o corpo é omitido. Sem configuração em nenhum dos dois, devolve 200 com `disponivel: False` (mesmo padrão de `saude_financeira`/liquidação — ausência de config manual é normal, não erro).
  - **Frontend**: `SotpSection.tsx` (`components/sections/`) — breakdown por segmento em tabela + barra empilhada proporcional ao EV de cada segmento, ponte até o Preço Justo (SOTP) comparado ao preço atual (upside %). Só renderiza quando `disponivel: true` (mesmo padrão de renderização condicional do `ConcessaoSection` — silencioso pra tickers sem configuração, não é um estado de erro visível).
  - **Critério de aceite confirmado ao vivo**: SIMH3 → cada segmento individualmente sensato (EV = EBITDA × múltiplo, auditável célula a célula: Vamos R$21,00bi = 3.000×7,0; Movida R$15,00bi = 2.500×6,0; JSL R$11,70bi = 1.800×6,5) ANTES de somar — EV consolidado R$47,70bi, Equity bruto R$32,70bi (− R$15bi de dívida), pós-desconto de holding (15%) R$27,80bi, preço justo R$92,65/ação. Confirmado ao vivo no navegador (não só via `curl`) — valores idênticos aos calculados manualmente, batendo exatamente com os testes automatizados.
  - Testes: `tests/test_sotp.py` (17 novos — cada método de segmento isolado incl. paridade exata com `calcular_dcf()` chamado direto, WACC≤g tratado, configuração sintética de 3 segmentos com soma/desconto de holding calculados à mão, segmento com erro não trava a soma, `num_acoes=0` não quebra, carregamento do JSON incl. ticker não mapeado/arquivo ausente retornando `None` explícito). Suíte completa: **365 passed** (348 pré-existentes + 17 novos).
  - **Limitação conhecida**: nenhuma pro mecanismo em si — a limitação real é a mesma já documentada no módulo (nenhum provider reporta dado por segmento), por isso o JSON de configuração é manual e só cobre os tickers que o usuário populado explicitamente.

- **Fase 4 — Scorecard Qualitativo: ✅ Completa (última fase da iniciativa de Novas Metodologias de Valuation).**
  - **`scorecard_qualitativo.py`** (novo módulo, raiz de `backend/`): `ScorecardQualitativo` (dataclass, 5 dimensões 0-10 — moat, gestão, concentração de clientes, risco regulatório, poder de precificação — cada uma com a escala documentada na docstring pra consistência entre análises ao longo do tempo; valida a faixa 0-10 no `__post_init__`, levanta `ValueError` fora dela) + `calcular_ajuste_qualitativo()` + `aplicar_ajuste_ao_score()`.
  - **Decisão de modelagem — ajuste ADITIVO, não multiplicativo, documentada explicitamente**: um ajuste multiplicativo (`score_ajustado = score_base × fator`) trava qualquer ativo com `score_base=0` em 0 pra sempre (0 × qualquer fator = 0), mesmo com o melhor scorecard qualitativo possível — indefensável, já que `score_base=0` é um valor legítimo da escala (não "erro"). O ajuste aditivo (`ajuste = (média_das_5_dimensões − 5.0) / 5.0 × teto`) não tem essa patologia — média=5 (neutro) não move nada, média=10 move o máximo pra cima, média=0 move o máximo pra baixo.
  - **Teto de `1.5` pontos (15% da escala 0-10) — critério de aceite ("nunca dominar o resultado quantitativo") cumprido e testado no limite**: mesmo com o scorecard qualitativo mais perfeito possível (10/10 em tudo), um ativo "Cara/Evitar" (`score_base=1.0`) sai em no máximo `2.5` — longe de "Muito Atrativa" (≥8). Magnitude escolhida pra ficar na mesma ordem de grandeza das penalizações já existentes no projeto (`valuation/endividamento.py`: 0.5 a 2.5 pontos; `valuation/risco.py::PENALIZACAO_*`), não inventada do zero. Score final sempre clampado em `[0, 10]` (cobre o caso de `score_base` já estar perto de uma borda).
  - **Endpoint**: `POST /api/valuation/{ticker}/scorecard` (+ `/valuation/{ticker}/scorecard`) — recebe `score_base` (o Score de Atratividade REAL daquele ticker, já calculado por `valuation()` e presente na tela quando o usuário mexe nos sliders) + as 5 dimensões, devolve o ajuste. **Decisão de arquitetura, investigada antes de implementar**: o pedido original citava "expor endpoint para salvar/recuperar o scorecard" e "reaproveitar o mecanismo de localStorage/persistência já usado no Watchlist... **ou** o backend equivalente se já tiver migrado para armazenamento server-side — verifique o estado atual antes de decidir". Verificado: a Watchlist AINDA é 100% localStorage (Fase 3 do Roadmap principal, "Supabase + Auth", segue 🔲 não iniciada) — não haveria motivo pra introduzir persistência server-side isolada só pro scorecard, à frente do resto do app. **Decisão**: o endpoint é stateless (só calcula, não salva nada em disco/banco); a persistência entre sessões usa localStorage no frontend, exatamente como a Watchlist.
  - **Frontend**: `ScorecardQualitativo.tsx` (`components/sections/`, renderizado como `ScorecardQualitativoSection`) — 5 sliders 0-10 com a descrição de cada extremo da escala visível abaixo do slider (mesmo texto da docstring do backend, pra o usuário calibrar a nota do mesmo jeito nas duas pontas). Persistência via `localStorage` com chave `vt_scorecard_<TICKER>` — mesmo padrão exato de `WATCHLIST_KEY` (`vt_watchlist`) já usado em `App.tsx`. Busca do ajuste debounced (300ms) a cada mudança de slider, exibindo Score base / Ajuste / Score ajustado logo abaixo — card posicionado imediatamente após a Matriz de Consenso (`SecaoConsenso`) na tela de análise, deixando explícito que o ajuste se aplica sobre ELA, não um score paralelo solto em outro canto da tela.
  - **Confirmado ao vivo no navegador**: BEEF3 (`score_base=3.0`) com os 5 sliders levados a 10/10 → "Score base 3.0 / Ajuste qualitativo +1.50 / Score ajustado 4.5" — bate exato com o teto documentado. Reload da página com o MESMO ticker confirmou a persistência: `localStorage.getItem('vt_scorecard_BEEF3')` devolveu as 5 notas salvas (10/10 em todas), não os defaults.
  - Testes: `tests/test_scorecard_qualitativo.py` (15 novos — defaults neutros, nota fora de [0,10] levanta erro, extremos (todas 0 → ajuste=−teto exato; todas 10 → ajuste=+teto exato), notas mistas calculadas à mão, monotonicidade do ajuste em relação à média, teto customizável, score ajustado nunca transforma "Cara/Evitar" em "Muito Atrativa" nem vice-versa, clamp em [0,10] nas duas bordas). Suíte completa: **380 passed** (365 pré-existentes + 15 novos).
  - **Limitação conhecida**: nenhuma. O endpoint é deliberadamente stateless (ver decisão de arquitetura acima) — não é uma limitação, é a decisão correta dado o estado atual do projeto (sem Supabase/Auth ainda).

## ✅ Novas Metodologias de Valuation — as 4 fases concluídas

Cenários & Sensibilidade → Valor de Liquidação → SOTP → Scorecard Qualitativo, nesta ordem, cada uma com checkpoint e suíte completa rodada antes de avançar. Suíte final: **380 passed** (0 pré-existentes quebrados). Nenhuma fase exigiu reverter ou refazer uma anterior.

## 🗺️ Roadmap

| Fase | Descrição | Status |
|------|-----------|--------|
| 1 | Motor de valuation completo (todos os métodos + frontend) | ✅ Completa |
| 2 | Watchlist (localStorage) + Deploy Railway | ✅ Completa |
| 2.5 | Saúde Financeira via dados CVM (DRE/DFC trimestrais) | ✅ Completa |
| 2.6 | FCFE — Valuation de Equity via CVM (ponta a ponta) | ✅ Completa |
| 3 | Supabase + Auth (watchlist por usuário, multi-device) | 🔲 Próxima |
| 4 | Produto: relatório PDF, comparativo entre tickers | 🔲 Futura |

## 🎯 Próximo passo (foco do próximo chat)

**Fase 3 — Supabase + Auth:**
- Criar projeto no Supabase Cloud
- Trocar `localStorage` por tabela Supabase (`watchlist` por usuário)
- Adicionar login com email/senha ou Google OAuth
- Multi-device: watchlist sincronizada em qualquer dispositivo
- Variável de ambiente `SUPABASE_URL` e `SUPABASE_ANON_KEY` no Railway

## 📋 Notas sobre dados CVM

URLs reais dos ZIPs (um por ano, contém todos os CSVs):
```
https://dados.cvm.gov.br/dados/CIA_ABERTA/DOC/ITR/DADOS/itr_cia_aberta_{ano}.zip
https://dados.cvm.gov.br/dados/CIA_ABERTA/DOC/DFP/DADOS/dfp_cia_aberta_{ano}.zip
```
CSVs extraídos de cada ZIP: `DRE_con`, `DFC_MI_con`, `BPP_con`
Cadastro: `https://dados.cvm.gov.br/dados/CIA_ABERTA/CAD/DADOS/cad_cia_aberta.csv`
